# ══════════════════════════════════════════════════════════
#  Streamlit Community Cloud 전용 의존성
#
#  Cloud 배포 시 앱 설정에서 이 파일을 지정하세요.
#  로컬(ngrok) 운영은 그대로 requirements.txt 를 씁니다 — 두 파일은
#  '무엇을 뺐는가' 만 다르고 버전 핀은 동일합니다.
#
#  왜 따로 두는가
#  ───────────────────────────────────────────────────────────
#  requirements.txt 를 그대로 올리면 RAG 의존성이 torch 를 끌고 온다.
#  실측 496MB — onnxruntime · faster-whisper 까지 합치면 Community Cloud 의
#  자원 한도에서 설치가 타임아웃되거나 실행 중 메모리로 죽는다.
#  아래는 '대시보드가 뜨고 탐지·분석이 도는' 최소 집합이다.
#
#  이걸 쓰면 꺼지는 기능 (전부 코드에서 선택 기능으로 가드돼 있음)
#  ───────────────────────────────────────────────────────────
#    · 📚 RAG 문서 검색      — chromadb · sentence-transformers 제외
#    · 🎙 음성 입력(로컬 STT) — faster-whisper 제외
#    · ONNX / PMML 모델 로더  — onnxruntime · sklearn-pmml-model 제외
#      (LightGBM·sklearn·joblib 번들은 그대로 동작한다)
#
#  대시보드 상단에 "설치 안 된 패키지" 안내가 뜨는 건 정상이다 —
#  경고일 뿐 앱은 그대로 돈다.
# ══════════════════════════════════════════════════════════

# ── 대시보드 ──
streamlit>=1.58,<2       # st.tabs(key/default) 가 1.58+ 다. 낮추면 탭에서 즉사한다
plotly>=5.18

# ── 핵심 ──
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3              # 배포 번들(.pkl)이 joblib 형식 — bundle_io.safe_load 가 사용

# ── LLM (클라우드 제공자만) ──
#  ⚠️ Cloud 에는 llama.cpp 서버가 없다. USE_LLM_PROVIDER=local 로 두면
#     LLM 분석이 전부 폴백 템플릿으로 떨어진다. secrets 에서 바꿔 두세요.
anthropic>=0.40
openai>=1.40

# ── 유틸 ──
python-dotenv>=1.0
requests>=2.31

# ── 데이터 ──
pyarrow>=15.0            # Parquet (X_tr/y_tr 분할셋)
duckdb>=0.10             # SQL 스코어링 모델
