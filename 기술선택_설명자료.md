# 기술 선택 설명 자료 — 예상 질문 대비

> **한 줄 입장**
> 계획서의 MCP·n8n은 *수단*이었고, 목표는 **"무인 실시간 탐지·알림"** 이었습니다.
> 목표는 100% 달성했고, 수단은 검증을 거쳐 더 적합한 쪽을 택했습니다.
> **검토하지 않은 것이 아니라, 검토하고 선택한 것입니다.**

---

## 0. 먼저 정리할 프레임

| 흔한 오해 | 실제 |
|---|---|
| "계획한 걸 못 했다" | 계획한 **목표**는 전부 달성. 수단만 교체 |
| "MCP를 안 했다" | `notifier_mcp.py` **프로토타입 구현·검증 완료**. 채택만 보류 |
| "n8n을 안 했다" | 도입 조건을 정의하고, 그 조건이 아직 아니라고 판단 |
| "나중에 못 붙인다" | 코어가 헤드리스라 어댑터만 추가하면 됨 (이미 3개가 붙어 있음) |

**근거 문서** — 회의용으로 급조한 게 아니라 작업 중 남긴 기록입니다.
`README_WATCHER.md` §11 설계 결정 기록 · §12 하지 않기로 한 것 ·
`PIPELINE_DIAGRAM.md` 이전 대비 변화표

---

## 1. "MCP 쓴다더니 왜 없습니까?"

### 30초 답변
> MCP는 **사람이 Claude를 통해 시스템에 말을 거는** 통로입니다. 방향이 반대예요.
> 저희가 필요한 건 **시스템이 사람에게 먼저 알리는** 것이라 맞지 않았습니다.
> 실제로 `notifier_mcp.py`를 만들어 검증했고, stdio 방식이라 **상시 데몬이 호출할 수 없다**는
> 구조적 한계를 확인했습니다. 그래서 알림 경로에서는 뺐습니다.

### 근거
- MCP stdio = Claude Desktop이 프로세스를 띄우고 도구를 호출하는 구조
- 워처는 **자기가 먼저** Slack을 호출해야 함 → MCP가 낄 자리가 없음
- 프로토타입 `notifier_mcp.py`는 `pipeline/notifier.py`의 **복제본**이었음
  (STARTTLS 폴백·오류 분류·`last_error` 진단이 빠진 구버전) → 로직 이중화 위험

### 꼬리 질문 대비
**Q. 그럼 MCP는 영영 안 씁니까?**
> 아니요. **조회·질의 용도로는 적합합니다.** "오늘 워처가 뭐 잡았어?"를
> Claude Desktop에서 묻는 형태죠. `DetectService`를 감싸면 되고,
> 남은 일 목록에 이미 올라가 있습니다.

**Q. 그럼 계획서가 틀린 겁니까?**
> 계획 단계에서는 "무인 자동화를 어떤 수단으로 구현할지"가 열려 있었고
> MCP는 유력 후보였습니다. 구현하면서 각 기술의 **역할이 명확해진** 것입니다.
> 후보를 나열하고 검증 후 선택하는 것이 정상 절차라고 봅니다.

---

## 2. "n8n은요? 워크플로 자동화 하신다더니"

### 30초 답변
> n8n을 지금 넣으면 **늘어나는 기능은 0, 늘어나는 운영 부담은 1**입니다.
> 탐지부터 발송까지가 이미 완결돼 있어서 n8n이 할 일이 없습니다.
> 대신 **언제 도입할지 조건을 정의해뒀습니다.**

### 도입 조건 (이 중 하나라도 생기면 즉시 검토)
- **에스컬레이션** — 10분 내 담당자 미확인 시 팀장 전화
- **업무시간 라우팅** — 주간 Slack / 야간 SMS
- **티켓 자동 생성** — Jira·Notion에 대응 건 등록
- **승인 워크플로** — 계좌 정지 전 다단계 결재

### 붙이는 비용
> 워처에 `--webhook-url` 옵션 하나 추가하면 됩니다.
> Slack 대신 n8n으로 쏘고, 라우팅은 n8n이 맡는 구조.
> **미리 만들지 않은 것이지, 못 만드는 것이 아닙니다.**

### 꼬리 질문 대비
**Q. 지금 만들어두면 안 됩니까?**
> 서버 한 대를 24시간 더 운영해야 하고, 장애 지점이 하나 늘어납니다.
> 현재 하루 알림이 **2.8건**이라 라우팅 규칙이 필요한 규모가 아닙니다.
> 실제로 필요해질 때 만드는 편이 유지보수 비용이 낮다고 판단했습니다.

---

## 3. "그럼 뭘 하셨습니까?" — 계획 대비 달성표

| 계획서의 목표 | 결과 | 검증 |
|---|---|---|
| 폴더 실시간 감시 | ✅ 5초 폴링 워처 · 신규+append 모두 | 커서 추적으로 재처리·누락 0 |
| ML 1차 판단 | ✅ LightGBM 13-class · 58피처 | `verify_bundle` 4경로 macro F1 0.6138 일치 |
| 임계값 초과 시 AI 분석 | ✅ 이중 임계값 + LLM 3단계 | 실측표로 임계값 확정 |
| 메시지 발송 | ✅ Slack + Email (등급별) | 실제 발송 확인 |
| **계획에 없던 추가분** | | |
| 판정 엔진 단일화 | ✅ `DetectService` | 대시보드·워처가 같은 코어 |
| 무인 운영 안전장치 | ✅ 기동 거부·서킷·중복 억제 | 시나리오 테스트 |
| 상태 관측 | ✅ 대시보드 패널 + 배지 | |
| 자가 복구 | ✅ 데드맨 스위치 + 예약 재개 | 시나리오 8종 검증 |
| AI 에이전트 확장 | ✅ 17 → 21액션 + 자가진단 | |

---

## 4. "기술 선택이 보수적인 것 아닙니까?"

### 30초 답변
> 유행하는 기술을 피한 게 아니라, **이 문제에 맞는 것을 골랐습니다.**
> 실제로 채택한 것들도 단순하지 않습니다.

| 채택 | 왜 |
|---|---|
| 로컬 LLM (llama.cpp) | 금융 데이터의 외부 전송 차단 |
| RAG (Chroma + KR-SBERT) | 판정 근거를 문서로 제시 |
| 서킷 브레이커 | LLM 장애 시 1건당 12초 낭비 → 알림 중단 방지 |
| 설정 핫 리로드 | 무중단 임계값 조정 |
| 데드맨 스위치 | 감시자의 감시 |

### 대표 사례 — watchdog 대신 폴링을 택한 이유
> 이벤트 방식(watchdog)이 더 "현대적"으로 보이지만,
> Windows의 `ReadDirectoryChangesW`는 **네트워크 공유(SMB)에서 이벤트를 놓칩니다.**
> 그것도 로그 없이 조용히요. 감시 폴더가 사내 공유가 될 가능성이 높아
> 5초 폴링을 택했습니다. **놓치지 않는 것이 세련된 것보다 중요합니다.**

---

## 5. "확장성은 어떻습니까?" — 이게 진짜 답입니다

가장 중요한 결정은 **판정 로직을 `DetectService` 하나로 모은 것**입니다.

```
              pipeline/detect_service.py   ← 판정 엔진 (Streamlit 무관)
                        │
     ┌──────────┬───────┴────────┬──────────────┐
  대시보드     워처            (n8n용 API)     (MCP 서버)
   [완료]     [완료]            [미착수]        [프로토타입]
```

- 이미 **2개 어댑터가 같은 코어에 붙어 동작 중**입니다 (증명 완료)
- n8n·MCP는 **세 번째, 네 번째 어댑터**일 뿐입니다
- 만약 판정 로직을 복붙했다면 어댑터를 늘릴수록 판정이 갈라졌을 겁니다

> **"안 붙인 것"과 "못 붙이는 것"은 다릅니다. 붙일 수 있게 만들어 뒀습니다.**

---

## 6. 먼저 밝힐 한계 (숨기면 나머지 신뢰가 깎입니다)

| 한계 | 현황 | 계획 |
|---|---|---|
| **실데이터 검증 미완** | 임계값은 검증셋(과거) 기준 | 운영 첫 주 실측과 대조 후 조정 |
| **미탐 4개 유형** | `l` 38.1% · `e` 30.4% · `c` 25.9% · `i` 26.1% | 임계값으로는 해결 불가 — 재학습·룰 보완 영역 |
| **유형 정확도 53.5%** | 탐지는 82.9%지만 유형 특정은 절반 | 담당자 확인 절차로 보완 중 |
| **llama.cpp 수동 기동** | 런처로 순서 보장, 서비스 등록은 미완 | 24시간 운영 전환 시 함께 등록 |
| **Streamlit Cloud 제약** | 클라우드 배포판은 워처 상태 조회 불가 | 외부 상태 저장소 필요 |

> 이 항목들은 **문서에 이미 기록돼 있습니다** (`README_WATCHER.md` §12 남은 일).
> 발견하고 적어둔 것이지, 지적받고 알게 된 것이 아닙니다.

---

## 7. 회의 중 기억할 3문장

1. **"목표는 달성했고, 수단은 검증해서 골랐습니다."**
2. **"MCP는 만들어봤고 방향이 맞지 않아 뺐습니다. n8n은 도입 조건을 정의해뒀습니다."**
3. **"안 붙인 것이지 못 붙이는 것이 아닙니다 — 코어 하나에 어댑터 2개가 이미 붙어 있습니다."**

---

## 8. 하지 말아야 할 답변

| ❌ | ✅ |
|---|---|
| "시간이 없어서 못 했습니다" | "우선순위를 이렇게 판단했습니다" |
| "그건 나중에 하겠습니다" | "이 조건이 생기면 도입합니다" (조건 명시) |
| "MCP는 별로예요" | "이 용도에는 방향이 맞지 않습니다. 조회 용도로는 적합합니다" |
| "다 됩니다" | 한계를 먼저 말하고 계획을 붙임 |

> 방어적으로 들리는 순간 변명이 되고, **판단 기준을 말하면 설계가 됩니다.**
