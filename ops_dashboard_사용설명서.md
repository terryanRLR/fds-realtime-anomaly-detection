# 🛡 FDS 관제 콘솔 (ops_dashboard.py) 사용 설명서

> 무인 워처가 24시간 올리는 이상거래 알림을 **사람이 판정**하고, 그 판정으로
> 임계값을 조정하는 운영 도구입니다.

> **문서 지도**
> · **이 문서** — 관제 콘솔 실무자 매뉴얼 (화면을 보며 읽는 문서)
> · `PATCH_NOTES5.md` — 버전별 변경 내역 (개발자용, 왜 그렇게 만들었는지)
> · `OPS_BACKLOG.md` — **아직 안 한 일만** 모아 둔 목록
> · `README_WATCHER.md` — 워처·관제 운영 문서
>
> *기준 버전: `ops_dashboard v37` · 최종 갱신 2026-08-10*

---

## 목차

1. [이게 무슨 도구인가](#1-이게-무슨-도구인가)
2. [시작하기 — 처음 5분](#2-시작하기--처음-5분)
3. [화면 구조](#3-화면-구조)
4. [사이드바 — 모든 설정](#4-사이드바--모든-설정)
5. [탭별 상세 사용법](#5-탭별-상세-사용법)
6. [하루 운영 흐름](#6-하루-운영-흐름)
7. [여러 명이 함께 쓸 때](#7-여러-명이-함께-쓸-때)
8. [문제 해결](#8-문제-해결)
9. [용어집](#9-용어집)
10. [파일·모듈 지도](#10-파일모듈-지도)
11. [이번 개선분 (v29 → v36)](#11-이번-개선분-v29--v36)
12. [미탐 반영 곡선 · 압축 탭 (v37)](#12-미탐-반영-곡선--압축-탭-v37)
13. [키보드 단축키 · AI 액션 확장 (v39)](#13-키보드-단축키--ai-액션-확장-v39)

---

## 1. 이게 무슨 도구인가

### dashboard.py 와의 차이

|  | `dashboard.py` (분석용) | `ops_dashboard.py` (운영용) |
|---|---|---|
| 목적 | **분석·검증** — 모델 성능을 보고, 합성 데이터를 만든다 | **운영** — 알림을 판정하고 임계값을 조정한다 |
| 데이터 소스 | 검증셋 (test.csv · parquet) | **라이브 DB** (`fds_results.db`) |
| 화면 수명 | "열어서 본다" | **"켜 둔다"** |
| 주 사용자 | 분석가·발표자 | **관제 담당자** |
| 포트 | 8501 | **8502** |

두 도구는 같은 DB와 같은 판정 엔진(`DetectService`)을 공유합니다.

### 이 도구만 할 수 있는 것

1. **오탐 피드백 루프** — 알림이 맞았는지 틀렸는지 사람이 기록합니다
2. **실제 판정 기반 임계값 튜닝** — 검증셋이 아닌 운영 분포로 계산합니다
3. **자동 갱신** — `st.fragment`로 관제 패널만 부분 리런합니다

### ⚠️ 실행 위치가 중요합니다

**워처가 도는 그 PC에서** 열어야 합니다. `fds_results.db`가 로컬 파일이라,
Streamlit Cloud 등 다른 서버에서는 워처 상태가 보이지 않습니다.

---

## 2. 시작하기 — 처음 5분

### 실행

```bat
run_ops_dashboard.bat
```

또는 직접:

```bash
streamlit run ops_dashboard.py --server.port 8502
```

브라우저에서 `http://localhost:8502` 로 접속합니다.

> 💡 `dashboard.py`(8501)와 동시에 켤 수 있습니다. 화면 우상단의
> `🔌 ops_dashboard.py · :8502` 배지로 어느 앱인지 구분하세요.
> **두 탭에서 같은 배지가 보이면 포트 충돌**입니다 — 둘 중 하나는 실제로 안 떠 있습니다.

### 첫 실행 시 안내 팝업

처음 열면 사용 안내 팝업이 뜹니다. 한 번 보면 다시 뜨지 않으며,
언제든 **사이드바 → `🎓 사용 안내 다시 보기`** 로 다시 열 수 있습니다.

### 반드시 먼저 할 일 3가지

| 순서 | 할 일 | 왜 |
|---|---|---|
| 1 | 사이드바 → **검토자** 를 내 이름으로 | 판정 기록·잠금·임시저장이 전부 이 이름으로 묶입니다 |
| 2 | 화면을 **아무 곳이나 한 번 클릭** | 브라우저가 소리를 막고 있습니다. 클릭 한 번이면 경보음이 자동으로 켜집니다 |
| 3 | **🔄 교대 인수인계** 탭에서 앞사람 메모 확인 | 지금 상황을 30초에 파악합니다 |
| 4 | **🚨 알림 트리아지** 에서 위에서부터 처리 | 기본 정렬이 대기순이라 가장 오래 방치된 건이 맨 위 |

---

## 3. 화면 구조

```
┌────────────────┬──────────────────────────────────────────────┐
│  사이드바       │  🛡 FDS 관제 콘솔    [미판정 12][SLA초과 3]   │
│  👤 판정자 홍길동│                      [최장대기 2시간][판정자] │
│  🛰 워처 🟢 정상 │ ┌──────────────────────────────────────────┐ │
│  ───────────    │ │ 🧠AI │🚨트리아지 │🟢실시간 │🔄교대 │... │ │
│  🤖 AI 어시스턴트│ ├──────────────────────────────────────────┤ │
│  ───────────    │ │                                          │ │
│  🛡 관제 설정    │ │            (선택한 탭 내용)               │ │
│    검토자·SLA   │ │                                          │ │
│    잠금·마스킹  │ │                                          │ │
│    📁 경로      │ │                                          │ │
│  🎯 임계값       │ │                                          │ │
│  🧠 탐지 모델    │ │                                          │ │
│  📂 데이터셋     │ │                                          │ │
│  ⚙ 고급 설정 ▸  │ │                                          │ │
│  🎓 사용 안내    │ │                                          │ │
└────────────────┴──────────────────────────────────────────────┘
```

### 헤더 배지 — 어느 탭에 있든 밀린 일이 보입니다

헤더 오른쪽에 **미판정 · SLA 초과 · 최장 대기 · 판정자** 4개가 상시 표시됩니다.
첫 화면이 `🧠 AI 분석`이라, 예전에는 트리아지 탭을 눌러야만 밀린 건수를 알 수 있었습니다.

- 숫자는 15초마다, 그리고 **DB가 바뀌면 즉시** 갱신됩니다
- `미판정`이 0이면 초록, 1건 이상이면 빨강입니다
- `판정자`가 비어 있으면 노란색으로 경고합니다 — 그 상태로 찍은 판정은 이름 없이 남습니다
- 500건을 넘으면 `500+`로 표기합니다 (헤더 집계 상한)
- 화면이 좁으면(900px 미만) 배지는 숨겨집니다

탭은 **손이 자주 가는 순서**로 배치돼 있습니다.

| # | 탭 | 사용 빈도 |
|---|---|---|
| 1 | 🧠 AI 분석·알림 | **첫 화면** — 탐지·분석 작업대 |
| 2 | 🚨 알림 트리아지 | 매시간 ★ 판정 루프의 본체 |
| 3 | 🟢 실시간 감시 | 상시 |
| 4 | 🔄 교대 인수인계 | 하루 2회 |
| 5 | 🗃 탐지 로그 | 주 단위 |
| 6 | 📉 오탐 분석 | 주/월 |
| 7 | ⚙ 임계값 튜닝 | 월 단위 |
| 8 | 🩺 진단 | 고장시 |

### ⌨ 키보드 단축키

탭 번호가 그대로 단축키입니다 — `2`를 누르면 트리아지로 갑니다.

| 키 | 동작 |
|---|---|
| `1` ~ `8` | 해당 번호의 탭으로 이동 (위 표의 번호와 같습니다) |
| `←` `→` | 이전 / 다음 탭 |
| `C` | AI 어시스턴트 입력창으로 이동하고 **커서까지** 놓습니다 |
| `H` | 사용 안내(첫 실행 팝업) 다시 보기 |
| `?` 또는 `/` | **단축키 전체 목록** 창 |
| `R` | 지금 새로고침 (라이브 DB 재조회) |
| `A` | 자동 새로고침 켜기 / 끄기 |
| `V` | 탭 라벨 압축 모드 (§12.2) |
| `B` | 사이드바 펼치기 / 접기 |
| `T` | 테마 순환 |
| `L` | 언어 순환 (한국어 → English → 日本語 → 中文) |
| `Ctrl` + `/` | 단축키 힌트 토스트 |

**입력창·목록에 커서가 있을 때는 동작하지 않습니다.** 검색어를 타이핑하다
`a`를 눌렀다고 자동 새로고침이 꺼지면 안 되기 때문입니다. 한글 IME로 조합
중인 글자, 그리고 셀렉트박스·슬라이더에 포커스가 있을 때도 마찬가지로
단축키가 새어나가지 않습니다.

> 외울 필요는 없습니다. 화면을 열면 우측 하단에 힌트 토스트가 한 번 뜨고,
> 언제든 `?` 로 전체 목록을 볼 수 있습니다. **AI 어시스턴트에게
> *"단축키 알려줘"* 라고 해도 같은 창이 열립니다.**

---

## 4. 사이드바 — 모든 설정

조작 가능한 값은 **전부 사이드바 한 곳**에 있습니다.
탭 안에 같은 설정이 또 있으면 반드시 어긋나기 때문에, 의도적으로 한 곳으로 모았습니다.

### 배치 원칙 — 만지는 빈도 순

사이드바는 위에서 아래로 **3층**입니다.

| 층 | 무엇 | 얼마나 자주 |
|---|---|---|
| ① 상태 | 👤 판정자 · 🛰 워처 생존 | 설정이 아니라 '지금 어떤가' — 항상 보입니다 |
| ② 매일 | 🛡 관제 설정 · 🎯 임계값 · 🧠 모델 · 📂 데이터셋 | 근무마다 / 작업할 때 |
| ③ 1회 | ⚙ 고급 설정 (LLM·알림 채널·음성·표시·버전) | 대개 처음 한 번 |

> 🛡 **관제 설정이 맨 앞인 이유** — 근무를 시작할 때 가장 먼저 하는 일이
> '검토자 이름을 내 이름으로 바꾸기'인데, 예전에는 이게 8번째라 스크롤해야 닿았습니다.
> 이름이 틀린 채로 찍은 판정·잠금·임시저장은 되돌릴 수 없습니다.

> ⚙ **고급 설정을 접어도 값은 안 날아갑니다** — 접기는 화면에만 적용되고
> API 키·Webhook은 그대로 남습니다.

### 🤖 AI 어시스턴트 (상태 바로 아래)

토글을 켜면 **어느 탭에 있든** 챗봇에게 바로 물을 수 있습니다.

- 최근 4개 왕복이 보이고, 아래 입력창으로 질문합니다
- 라이브 DB를 실시간 조회해 답합니다 (검증셋이 아님)
- `↗ 전체보기` → AI 탭의 전체 대화·음성 입력으로 이동
- **AI 탭과 같은 대화를 공유**합니다 — 사이드바에서 묻다가 탭으로 넘어가도 맥락이 이어집니다

질문 예시:
```
지금 미처리 몇 건이야?
오탐률 어때?
워처 살아있어?
가장 오래 기다린 알림이 뭐야?
```

#### 물어보는 것만이 아니라, **시켜도 됩니다**

말로 화면을 조작할 수 있습니다. 아래는 갈래별 예시이고, 전체 목록은
AI 탭의 `🔧 에이전트 자가진단` 안에 있습니다.

| 시키는 말 | 일어나는 일 |
|---|---|
| *"트리아지로 가줘"* | 탭 이동 |
| *"SLA를 60분으로 바꿔줘"* | SLA 변경 |
| *"임계값 0.7로"* | 위험점수 임계값 변경 |
| *"SLA 넘긴 것만 골라줘"* | 초과분 **선택**까지 (저장은 사람이) |
| *"위험점수 순으로 정렬해줘"* | 트리아지 정렬 |
| *"미판정만 보기 꺼줘"* · *"50건씩 보여줘"* | 목록 필터·표시 건수 |
| *"최근 30일로 봐줘"* | 분석 기간 |
| *"탐지 로그에서 T20250810 찾아줘"* | 로그 검색 + 해당 탭 이동 |
| *"오탐을 판정자별로 보여줘"* | 오탐 분석 집계 기준 |
| *"자동 새로고침 꺼줘"* · *"5초마다"* | 실시간 감시 주기 |
| *"이거 분석 돌려줘"* · *"일괄 분석 실행"* | 분석 실행 (**발송은 하지 않습니다**) |
| *"단축키 알려줘"* | 단축키 목록 창 |

**할 수 없는 것 — 일부러 막아 두었습니다.**

| 막은 것 | 이유 |
|---|---|
| 알림 발송 (Slack · Email) | 되돌릴 수 없고 밖으로 나갑니다 |
| 정탐/오탐 **판정 기록** | 감사 대상입니다. 자연어는 *"오탐으로 찍어줘"* 와 *"오탐이면 어떻게 돼?"* 를 구분하지 못합니다 |
| 워처 시작 / 중지 | 무인 운전의 생사입니다 |
| 경보 등급 임계값 저장 | 화면 설정처럼 보이지만 `watcher_config.json` 은 **핫 리로드**라, 저장하는 순간 무인 워처의 경보 기준이 바뀝니다 |

이것들은 챗봇이 "제가 할 수 없습니다"라고 답하고 **어느 버튼을 누르면 되는지**
안내합니다. 사람이 직접 누르는 것이 설계입니다.

> 💡 **시켰는데 화면이 안 움직인다면** → AI 탭의 `🔧 에이전트 자가진단`을 여세요.
> 거기서 *"파서 정상 — N종 모두 인식됩니다"* 초록불이 뜨는데도 안 움직인다면
> 원인은 액션이 아니라 **LLM 연결**입니다(로컬 모델 미기동 등). 같은 패널의
> `액션 직접 실행해 보기`로 LLM을 거치지 않고 동작만 따로 확인할 수 있습니다.

### 🛡 관제 설정 ★ 근무 시작 때 여기부터

| 항목 | 기본값 | 설명 |
|---|---|---|
| **검토자** | 시스템 계정명 | 판정·잠금·임시저장이 이 이름으로 묶입니다 |
| **SLA(분)** | 30 | 알림 발생 후 이 시간 안에 판정한다는 약속 |
| **근무(시간)** | 8 | 교대 인수인계가 요약할 구간 |
| **🔒 동시 판정 잠금** | 켬 | 다른 담당자에게 "내가 보고 있다"를 표시 |
| 마스킹 레벨 | standard | `off` · `basic` · `standard` · `strict` |
| **🔔 이상거래 경보** | 켬 | 경보 마스터 스위치. 세부 설정은 `⚙ 경보 세부 설정` 버튼으로 |
| 📁 경로 | — | DB · 워처 로그 · 모델 폴더 · 워처 감시 폴더 |

> 📁 **경로가 관제 설정 안에 있는 이유** — `모델 폴더`를 바꾸면 바로 아래
> `🧠 탐지 모델` 목록이 **같은 화면에서** 갱신됩니다. 순서가 반대였을 때는
> 한 번 더 새로고침해야 반영됐습니다.

> SLA는 챗봇에게 시켜도 됩니다 — *"SLA를 60분으로 바꿔줘"*

### 🎯 위험점수 임계값

- **임계값 슬라이더** — 이 점수 이상이면 이상거래로 봅니다
- **📮 이중 임계값 발송** — 위험도 구간을 둘로 나눕니다
  - ① 검토 요청 임계값 이상 → **Slack만**
  - ② 확정 경보 임계값 이상 → **Slack + Email**
  - ②가 ①보다 낮으면 자동으로 `max(①,②)`로 보정됩니다

#### ⚠ 임계값은 3개입니다 — 이름이 같아도 뜻이 다릅니다

슬라이더 아래에 **대조표**가 항상 붙어 있습니다 (⚙ 임계값 튜닝 탭에는 설명 포함 전체판).

| 무엇을 정하나 | 어디서 바꾸나 | 언제 보나 |
|---|---|---|
| 🛰 **워처 경보 등급** | `watcher_config.json` · ⚙ 임계값 튜닝 탭 | 알림이 안 울릴 때 |
| 🎯 **탐지 판정 임계값** | 사이드바 🎯 임계값 | 이 화면에서 돌린 탐지 결과가 이상할 때 |
| 📮 **발송 등급** | 사이드바 📮 이중 임계값 | Slack/Email이 안 나갈 때 |

셋은 **서로 다른 것을 정하므로 값이 달라도 정상**입니다.
예전에는 셋 다 `th_review`/`th_confirm`이라 불리는데 각자 자기 화면에만 있어서,
사이드바가 `0.50`을 보여주는 동안 워처는 `0.005/0.9`로 돌아도 아무도 몰랐습니다.

### 🧠 탐지 모델

- 전역 탐지 모델을 고릅니다. **탐지 입력·재검증·진단이 모두 이 선택을 따릅니다**
- `● 로드 가능` / `● 파일 없음` 배지로 상태를 확인합니다
- 목록은 `models/` 폴더에서 자동 탐색됩니다 (배포 번들이 맨 위)

### 📂 데이터셋

- 데이터 폴더를 스캔해 csv·parquet를 찾습니다
- `🏷️` = 라벨(정답) 있음 / `❔` = 라벨 없음
- 여기서 고른 데이터셋이 **AI 탭 → 📂 선택 데이터셋** 입력 모드에서 쓰입니다

### ⚙ 고급 설정 (기본 접힘)

아래 4가지는 **대개 처음 한 번만** 정하는 값이라 한 덩어리로 접혀 있습니다.
접어도 값은 그대로 유지됩니다.

#### 🤖 분석 설정

| 항목 | 설명 |
|---|---|
| 제공자 | `local`(llama.cpp) · `anthropic` · `openai` · `deepseek` · `moonshot` · `custom` |
| RAG top_k | LLM 분석에 함께 넣을 참고 문서 개수 |
| API Key | 여기 입력한 값이 **`.env`보다 우선**합니다 |
| 로컬 PII 마스킹 생략 | 로컬 모델은 외부로 안 나가므로 마스킹을 건너뛰어 분석 품질을 높입니다 |

#### 📨 알림 채널

- Slack Webhook URL · 받는 사람 · SMTP User/Password
- **🚀 Slack 자동 발송** / **🚀 Email 자동 발송**
  - 되돌릴 수 없는 동작이라 **기본 OFF**입니다
  - 이중 임계값 모드에서는 검토 등급은 Slack만, 확정 등급만 Email이 나갑니다

#### 🗣 음성

- TTS 언어 — AI 분석문을 소리로 읽을 때의 음성
- 경보음 설정은 여기가 아니라 **🟢 실시간 감시** 탭에 있습니다
  (볼륨·조용한 시간·중복 억제가 한 묶음이라 쪼개면 헷갈립니다)

#### 🎨 표시

언어(한/영/일/중) · 테마 · ℹ 모듈 버전

### 🎓 사용 안내 다시 보기 (맨 아래)

처음 실행 때 떴던 온보딩을 다시 엽니다.

---

## 5. 탭별 상세 사용법

### 🚨 탭 2 — 알림 트리아지 ★

**이 앱의 존재 이유입니다.** 대부분의 시간을 여기서 씁니다.

#### 상단 필터

| 항목 | 설명 |
|---|---|
| 미판정만 | 아직 아무도 안 본 건만 (기본 켬) |
| 최소 점수 | 이 점수 미만은 숨김 |
| 표시 건수 | 10 / 20 / 30 / 50 (상한 50 — 행마다 위젯이 6개라 그 이상은 화면이 느려집니다) |
| **정렬** | **대기순**(기본) / 점수순 |
| **내림차순** | 켬(기본) = 오래 기다린 순 · 위험한 순 / 끔 = 반대 |

#### ☑️ 일괄 판정

같은 결론이 뻔한 묶음(테스트 계정 20건, 같은 배치 오류 15건)을 한 번에 처리합니다.

1. 알림 왼쪽 **체크박스**로 고릅니다 (펼치지 않아도 선택 가능)
2. `☑️ 모두 선택` / `⬜ 선택 해제` / `🔴 SLA 초과만` 버튼으로 한 번에 고를 수도 있습니다
3. 판정·사유·일괄 메모를 정하고 `💾 선택한 N건 일괄 판정`

> 🔒 다른 담당자가 잠근 건은 **자동으로 제외**되고 몇 건이 빠지는지 미리 알려줍니다.
> 일괄이라고 잠금을 무시하면 잠금을 둔 의미가 없기 때문입니다.
> 저장은 하나의 트랜잭션으로 묶여, 워처 폴링을 밀어내지 않습니다.

> 💡 **기본이 왜 대기순인가?**
> 점수 높은 건은 이미 누가 봤을 확률이 높고, 방치된 건은 아무도 안 봤습니다.
> 후자가 더 위험합니다.

#### SLA 현황 (KPI 4개)

```
  대기        SLA 30분 초과      임박        최장 대기
  23건        7건 ⚠확인필요      2건         6시간 40분
```

#### 알림 목록 읽는 법

```
🔴 0.930 · J 대포통장 · TXN_8821 · 2026-08-08 03:12 · ⏱ 1시간 30분 · 🔒 bob  💾
└┬┘ └─┬─┘  └────┬────┘  └───┬───┘  └───────┬───────┘   └────┬────┘  └─┬─┘  └┬┘
 │    │        │           │              │              │        │     └ 임시저장 있음
 │    │        │           │              │              │        └ 다른 사람이 검토 중
 │    │        │           │              │              └ 방치된 시간
 │    │        │           │              └ 탐지 시각
 │    │        │           └ 거래 ID
 │    │        └ 예측 사기 유형
 │    └ 위험 점수
 └ 🔴 SLA 초과 / 🟡 임박 / (없음) 정상
```

#### 판정하기

알림을 펼치면 왼쪽에 **거래 상세**(마스킹 적용), 오른쪽에 **판정 패널**이 나옵니다.

| 판정 | 의미 |
|---|---|
| ✅ 정탐 (tp) | 실제 사기가 맞았다 |
| 🟡 오탐 (fp) | 정상 거래인데 알림이 갔다 ← **이 대시보드의 주인공** |
| 🔴 미탐 (fn) | 알림이 안 갔는데 사기였다 (사후 제보) |
| ⏸ 보류 (unclear) | 판단 불가 / 추가 확인 필요 |

**오탐**을 고르면 사유를 함께 고릅니다:

| 코드 | 의미 |
|---|---|
| `legit_customer` | 정상 고객 확인됨 (본인 거래) |
| `known_pattern` | 기존 예외 패턴 (해외출장·급여일 등) |
| `test_data` | 테스트/내부 거래 |
| `data_error` | 데이터 오류·중복 유입 |
| `model_drift` | 모델 오작동 의심 (근거 불명) |
| `rule_overfit` | 룰 과민반응 |
| `other` | 기타 (메모 참조) |

> 자유 입력만 받으면 집계가 불가능해집니다. **왜 오탐이 나는가**를 유형별로 세야
> 임계값을 올릴지 피처를 고칠지 결정할 수 있습니다.

#### 🔒 잠금 · 💾 임시저장 (자동)

- **입력을 시작하면 자동으로 잠금**이 걸립니다 → 다른 담당자 화면에 `🔒 내이름`으로 표시
- **15분간 응답이 없으면 자동 해제**됩니다 (브라우저를 그냥 닫아도 영구 잠금이 되지 않게)
- 판정·사유·메모는 **입력 즉시 DB에 저장**됩니다 → 새로고침해도 그대로
- 임시저장은 **사람마다 따로** 보관됩니다 (같은 알림을 둘이 봐도 서로 안 덮어씀)
- **저장 버튼을 누르면** 판정이 기록되고 잠금·임시저장이 함께 정리됩니다

> ⚠️ 잠금은 **협조적 잠금**입니다. 물리적으로 막지 않습니다 —
> `🔓 잠금 무시하고 내가 검토` 로 넘어갈 수 있습니다.
> DB 레벨 강제 잠금은 워처의 쓰기까지 물릴 수 있어 위험하기 때문입니다.

#### 🔍 재검증

`재검증` 버튼을 누르면 **현재 모델**로 그 거래를 다시 판정합니다.
탐지 당시와 결과가 다르면 드리프트 해석이 함께 나옵니다.

---

### 🟢 탭 3 — 실시간 감시

켜 두는 상황판입니다.

- **자동 새로고침** 토글 + 간격(3·5·10·30·60초)
  - `st.fragment`로 이 패널만 부분 리런합니다 (전체 리런하면 모델·데이터셋 캐시까지 흔들립니다)
- 워처 생존 표시: 🟢 정상 / 🔴 응답 없음 / ⚫ 기록 없음
- KPI: 폴링 · 처리 행 · 이상거래 · 알림 발송 · 오류
- **실시간 피드** — 최근 이상거래 12건 (클릭하면 트리아지로 이동)

#### 🔔 경보 설정

| 항목 | 기본값 | 설명 |
|---|---|---|
| 경보 켜기 | **ON** | 관제 도구를 열어 둔 사람은 경보를 받으려고 연 것입니다 |
| 🔊 사운드 | ON | |
| 🖥 데스크톱 알림 / 💬 화면 팝업 | ON | |
| 울릴 등급 | `확정만` | `확정만` / `검토+확정` / `모든 이상거래` |
| 음량 | 0.25 | 야간 관제실 기준 |
| **삐- 반복 횟수** | 3 | 1~10. 관제실에서 3회는 짧고 10회는 고문입니다 |
| 재알람 억제 | 30분 | 같은 거래 N분 내 재알람 안 함 |
| 조용한 시간 | 사용 안 함 | 예) 22~7 = 밤 10시~아침 7시 무음 |

#### 🔔 경보가 뜨면

새 경보는 **화면 우상단**에 레이더 카드로 뜹니다 — 어느 탭에 있든 보입니다.

```
┌──────────────────────────────┐
│ ◜◝ 레이더    [확정]        ✕ │  ← ✕ 로 즉시 닫기
│  ⟳         0.930            │
│            F 위장인출        │
│            TXN_8821 · 20:00  │
│            클릭 → 탐지 로그   │  ← 카드 아무 곳이나 클릭
└──────────────────────────────┘
```

| 동작 | 결과 |
|---|---|
| **카드 클릭** (아무 곳이나) | 🗃 탐지 로그로 이동 — 그 거래의 당시 데이터·분석 |
| **✕** | 즉시 닫기 (마지막 카드를 닫으면 컨테이너도 사라짐) |
| 그냥 두면 | 확정 45초 · 그 외 22초 뒤 자동 소멸 |

**윈도우 알림을 클릭해도 🗃 탐지 로그로** 갑니다 — 알림을 누르는 사람이 보고 싶은 건
"그 건이 무엇이었나"(당시 데이터·분석)이기 때문입니다.

##### 알림 방식 4가지

| 토글 | 기본 | 설명 |
|---|---|---|
| 🔊 사운드 | ON | 삐- 반복 횟수 1~10 설정 |
| 🖥 데스크톱 알림 | ON | Windows 알림 (권한 필요) |
| 🛰 플로팅 카드 | **ON** | 우상단 레이더 카드 (기본 경보 UI) |
| 📋 상단 배너(폴백) | OFF | 엄격한 CSP 등으로 카드가 안 보일 때만 |

> ⚠️ 플로팅 카드와 상단 배너를 **둘 다 켜면 같은 경보가 두 번** 보입니다.

> 🔊 **소리는 화면을 한 번 클릭하면 자동으로 켜집니다.**
> 브라우저가 사용자 제스처 전에는 오디오를 막기 때문인데, 이제 아무 곳이나
> 클릭·키입력하면 자동 활성화됩니다. 수동으로 켜려면
> `🔔 소리·알림 활성화` 버튼을 누르세요.

#### 🩺 소리·알림 상태 진단

버튼을 누르면 브라우저 안의 상태를 읽어 보여줍니다.

| 항목 | 내용 |
|---|---|
| 오디오 | `running`(정상) / `suspended`(클릭 필요) / `none` |
| 데스크톱 알림 | `granted` / `denied`(자물쇠 아이콘에서 허용) / `default` |
| 경보 컨트롤러 | 주입 여부 |
| 표시 중인 카드 수 | 실시간 |
| 탭 상태 | 백그라운드면 알림 지연 가능 |
| 테스트 알림 | 권한이 있으면 실제로 1건 발사 |

#### 🔔 테스트 경보 3종

`🔴 확정` / `🟡 검토` / `🔵 관찰` — **등급 필터·조용한 시간을 무시하고** 무조건 표시합니다.
필터에 걸려 아무 일도 안 일어나면 고장인지 설정인지 구분할 수 없기 때문입니다.

**소음 예보**도 함께 나옵니다 — "하루 N회 알람, 그중 M회는 헛 알람 예상".
헛 알람이 하루 3회 이상이면 등급을 올리라는 경고가 뜹니다.
소음이 심하면 담당자가 반드시 알람을 꺼버리기 때문입니다.

#### ⚙ 워처 제어

워처 시작/중지/설정/로그를 이 안에서 다룹니다.

---

### 🔄 탭 4 — 교대 인수인계

#### 📥 앞 근무자가 남긴 것 (맨 위)

근무를 시작할 때 **가장 먼저 볼 것은 내 실적이 아니라 인계 사항**입니다.
최근 인수인계가 맨 위에 뜨고, 이전 것들은 접혀 있습니다.

#### 📊 근무 요약

```
 유입 알림    판정 완료    미처리 잔여        최장 대기
 47건        38건        9건 (SLA초과 3)    2시간 10분

 ✅정탐 12   🟡오탐 24   🔴미탐 1   ⏸보류 1
 오탐률 66.7% — 분모는 정탐+오탐입니다(보류·미탐 제외)
```

- **판정자별** 표 — 누가 몇 건을 어떻게 찍었나
- **넘기는 미처리** — 오래 기다린 순 상위 15건

#### 📤 인수인계 작성

메모를 적고 `💾 인수인계 저장` 을 누르면, **작성 시점의 요약 스냅샷이 함께 보관**됩니다.
나중에 숫자가 바뀌어도 "그날 무슨 상황이었나"가 남습니다.

`⬇ 인수인계서(.md)` 로 파일로 내려받아 메신저에 붙일 수도 있습니다.

---

### 🗃 탭 5 — 탐지 로그

지난 탐지를 거래 ID로 찾아 **그때 그 데이터와 그때 그 AI 분석**을 그대로 봅니다.

- 검색 (거래 ID 일부) · 건수 · 이상거래만
- 캐시 현황: `📼 1,234건 / 거래 890건 · 12.3MB (원본 45.6MB · 압축률 0.27)`

> 📼 **분석 캐시란?**
> 탐지 시점의 LLM 리포트·확률분포·환경은 **그 프로세스 메모리에만** 존재합니다.
> 워처에 붙인 훅(`analysis_store.attach`)이 이걸 DB에 남깁니다.
> 훅이 없으면 근거가 Slack 발송과 함께 증발합니다(복원 불가).
>
> 캐시는 **이상거래로 판정된 건만** 남깁니다. 정상 거래는 LLM도 안 돌았고
> 남길 근거도 없어 용량만 먹기 때문입니다.

---

### 📉 탭 6 — 오탐 분석

쌓인 판정으로 "무엇이 헛 알람이었나"를 셉니다.

- 기간(`window_h`) 선택
- 오탐 사유별 분포
- 유형별·등급별 오탐률

> ⚠️ **커버리지가 낮으면 오탐률은 참고용입니다.**
> 판정한 건이 전체 알림의 일부라면, 검토가 쉬운 건에 판정이 몰렸을 수 있습니다.

---

### ⚙ 탭 7 — 임계값 튜닝

**검증셋이 아니라 담당자가 실제로 찍은 판정**으로 비용을 계산합니다.

| 입력 | 설명 |
|---|---|
| 오탐 비용 | 헛 알람 1건의 비용 (기본 30,000원) |
| 미탐 비용 | 놓친 사기 1건의 비용 (기본 3,000,000원) |

기대 비용이 최소가 되는 임계값을 추천하고, `적용` 하면 워처 설정에 반영됩니다.

> 세션2(dashboard.py)의 비용곡선은 **검증셋(정적 라벨)** 기준이라 운영 분포와 다릅니다.
> 여기 값이 진짜입니다.

---

### 🧠 탭 1 — AI 분석 · 알림 ★ 첫 화면

아직 판정되지 않은 **임의의 거래**를 즉석에서 탐지·분석합니다.

탭을 열면 바로 작업용 서브탭 4개(`🎯 탐지 입력 · 🔍 단건 분석 · 📦 배치 분석 · 🤖 AI 어시스턴트`)가
나옵니다. 프롬프트·RAG 편집기는 **그 아래**에 접혀 있습니다 — 이 탭에 오는 이유는
거의 항상 탐지·분석이지, 프롬프트 손보기가 아니기 때문입니다.

#### 서브탭 1 — 🎯 탐지 입력 (6가지 방식)

| 방식 | 설명 |
|---|---|
| **📂 선택 데이터셋** | **기본 탭.** 사이드바에서 고른 데이터셋 (parquet X+y 페어 포함) |
| ✏️ 직접입력 | 금액·거리·잔액·채널·OS 등을 폼으로 입력 |
| 📄 test.csv | 경로에서 N건 무작위 추출 |
| 📊 train.csv | 위와 동일 (라벨 포함) |
| 🧪 합성생성 | 유형을 지정해 가상 거래 생성 |
| 📁 폴더배치 | 폴더의 첫 CSV 첫 행 |

##### 🎯 추출 범위 (선택 데이터셋)

| 값 | 의미 |
|---|---|
| 🌐 전체 | 정상 + 사기 |
| 🚨 사기 전체 | a~l |
| ✅ 정상만 | m |
| 개별 유형 | a · b · … · l (유형명 함께 표시) |

특정 유형만 뽑아 모델이 어떻게 반응하는지 보는 것이 실제 조사 방식입니다.
라벨이 없는 데이터셋은 거를 근거가 없으므로 범위 선택이 숨겨집니다.

##### 💾 액션 바

추출하면 4개 버튼이 나옵니다.

| 버튼 | 동작 |
|---|---|
| 💾 CSV 저장 | 브라우저 다운로드 (내부 키 `_*` 제외) |
| 📤 inbox 전송 | 워처 감시 폴더에 저장 → **5초 내 자동 탐지·알림**까지 이어짐 |
| ▶ 탐지 실행 | 선택한 1건을 즉시 판정 |
| 📦 일괄 분석 (N건) | 추출분 전체를 `📦 배치 분석` 탭으로 넘김 |

> inbox 전송은 `.tmp` → 원자적 교체라 워처가 반쪽 파일을 읽지 않습니다.
> 배치 탭에서는 **분석 대상**을 `🚨 알림 큐`(이미 판정된 라이브 알림)와
> `📦 넘겨받은 추출분`(미판정 → 실제 모델로 재분류) 중에서 고릅니다.

탐지하면 다음이 나옵니다:

```
🚨 이상거래 탐지                              0.930 / 1.0
   모델 lgbm_13class · 임계값 0.50            [J 대포통장]
┌──────────────┬────────────────────────────────────────┐
│  위험 게이지   │  예측 유형 · 실제 정답 · 입력 방식      │
│   ◜◝ 0.930   │  위험 특징 태그                        │
└──────────────┴────────────────────────────────────────┘
📋 규칙 적합도  3/4  적합도 2.03
   ✅ 수취계좌 거래중지 이력 (j형 실측 100%)
   ✅ 미사용(휴면) 계좌 → 동시 충족이 j형의 지문
   ⬜ 거래 실패 또는 잔액부족
사기 유형 정보 · 유형별 확률 (막대 + 차트)
```

> ⚠️ **규칙 체크리스트는 사기/정상을 판정하지 않습니다.**
> 정상 거래도 같은 특징을 흔히 갖습니다(수취정지 49% · 미사용계좌 51% · 고액입금 42%).
> 규칙만으로 판정하면 정밀도가 0.3%입니다.
> "사기라고 할 때 **어느 유형 특징에 맞는가**"만 보여줍니다.

이어서 `🧠 AI 분석` 을 누르면 LLM이 원인 해설 + Slack 초안 + Email 초안을 만듭니다.

- 🔊 분석문 음성 재생
- 🔁 **단계별 재생성** — 마음에 안 드는 한 단계만 다시 (3단계 통 재생성은 최대 6분 45초)
- 🕘 **탐지 이력** — 임계값을 바꾸면 과거 판정이 몇 건 뒤집히는지

#### 서브탭 2 — 🔍 단건 분석

알림 큐에서 하나 골라 AI 분석합니다.

#### 서브탭 3 — 📦 배치 분석

여러 건을 한 번에 분석하고 요약 리포트를 만듭니다.

#### 서브탭 4 — 🤖 AI 어시스턴트

전체 대화 + 🎤 음성 입력. 사이드바 챗과 **같은 대화**입니다.

> 이 챗봇은 **읽기 전용 해설**만 합니다. 워처 시작/중지 같은 되돌릴 수 없는
> 조작은 별도의 확인 절차 설계가 필요해 의도적으로 막아 뒀습니다.

#### 🖊 프롬프트 편집기 · 📚 RAG 편집기 (서브탭 아래, 접힘)

분석/Slack/Email 프롬프트와 RAG 지식베이스 문서를 화면에서 직접 고칩니다 (재시작 불필요).

---

### 🩺 탭 8 — 진단

#### 🔌 연결 테스트

"왜 알림이 안 오지?"에 3초 안에 답합니다.

| 버튼 | 확인 내용 |
|---|---|
| 🧪 ML 모델 | 모델 파일 로드 가능 여부 |
| 📚 RAG | 지식베이스 검색 동작 |
| 🧠 LLM | 제공자 연결 (32토큰 / 12초 ping) |
| 📨 알림 | **실제 SMTP 접속·로그인** + Slack webhook 설정 |

> LLM 테스트는 전체 분석(최대 6분 45초)이 아니라 짧은 ping을 씁니다.

#### 📤 발송 감사 로그

자동·수동을 가리지 않고 Slack/Email로 나간 **모든 시도**를 기록합니다
(이번 세션 한정 · 최근 50건).

```
✅ 08-08 14:23:11 · slack · J형 0.9300 · webhook · mask=standard · auto
❌ 08-08 14:25:03 · email · A형 0.8710 · ops@x.com · mask=standard · manual
```

외부로 나간 것은 회수할 수 없으므로, 누가·언제·어디로·어떤 마스킹으로
보냈는지는 반드시 남깁니다.

#### 그 밖에

- 타임존 진단 (UTC/로컬 혼재 검사)
- 모델 상태
- 모듈 버전 목록

---

## 6. 하루 운영 흐름

### 근무 시작 (5분)

1. 사이드바 **맨 위** — `👤 판정자`가 내 이름인지, `🛰 워처`가 🟢인지 한눈에 확인
   (다르면 바로 아래 **🛡 관제 설정 → 검토자** 에서 변경)
2. 헤더 배지로 **밀린 양** 파악 — `미판정` · `SLA 초과` · `최장 대기`
3. **🔄 교대 인수인계** → 앞사람 메모 읽기
4. **🟢 실시간 감시** → 자동 새로고침 켜기 + `소리·알림 활성화` 클릭

### 근무 중 (상시)

- **🚨 트리아지** 를 기본 탭으로 두고, 위에서부터 판정
- 🔴 SLA 초과 건 우선
- 모르는 건은 **⏸ 보류** 로 두고 메모를 남기세요 (임시저장은 자동)
- 판단이 어려우면 **🧠 AI 분석** 또는 사이드바 **🤖 AI 어시스턴트**

### 근무 종료 (5분)

1. **🔄 교대 인수인계** → 요약 확인
2. 특이사항을 메모에 적기 (보류 건은 반드시 이유를 남기세요)
3. `💾 인수인계 저장`

### 주 단위

- **📉 오탐 분석** → 오탐 사유 분포 확인
- 특정 사유가 몰리면 원인 조치 (임계값 / 피처 / 룰)

### 월 단위

- **⚙ 임계값 튜닝** → 실판정 기반 추천치 확인 후 적용

---

## 7. 여러 명이 함께 쓸 때

### 반드시 지킬 것

1. **각자 검토자 이름을 다르게** 설정하세요
   - 같은 이름이면 잠금이 서로를 구분하지 못합니다
2. `🔒 동시 판정 잠금` 을 켜 두세요 (기본 켬)
3. `🔒 다른이름` 이 보이면 **먼저 확인**하고 넘어가세요

### 잠금이 동작하는 방식

```
alice 가 TXN_001 에 입력 시작
   → alert_claim 테이블에 (TXN_001, alice, 지금) 기록
   → bob 화면에 "🔒 alice 님이 3분 전부터 검토 중입니다"

alice 가 저장          → 잠금 자동 해제
alice 가 브라우저 닫음  → 15분 후 자동 만료 → bob 이 가져갈 수 있음
```

### 임시저장은 사람마다 따로입니다

같은 알림을 alice와 bob이 동시에 보고 있어도, 각자의 메모는 섞이지 않습니다.

---

## 8. 문제 해결

### ❌ "DB를 찾을 수 없습니다"

- **워처가 도는 PC에서 열었는지** 확인하세요
- 사이드바 → `🛡 관제 설정` → `📁 경로` → DB 경로 수정

### ⚠️ "응답 없음 — 마지막 폴링이 N시간 전입니다"

워처가 죽었거나 멈췄습니다.

1. **🟢 실시간 감시** → `⚙ 워처 제어` 에서 상태·로그 확인
2. 필요하면 재시작 (`run_watcher.bat`)

### 📼 "분석 캐시는 정상 연결돼 있습니다 — 아직 담긴 게 없을 뿐입니다"

**정상입니다.** 훅은 붙어 있고, 캐시할 이상거래가 아직 없다는 뜻입니다.
캐시는 워처가 **이상거래로 판정한 건**만 남깁니다.

- `inbox/` 가 비어 있거나
- 처리한 행이 전부 정상이었거나

`🟢 실시간 감시` 탭 KPI의 **처리 행 / 이상거래** 수치로 확인하세요.

> 반면 `캐시 훅이 아직 붙지 않았습니다` (노란 경고)가 뜨면 진짜로 훅이 없는 것입니다.
> `watcher.py` 의 `DetectService` 생성 직후에 2줄을 추가하세요.

### 🔇 경보음이 안 울립니다

1. 화면 아무 곳이나 **한 번 클릭**하세요 (자동 활성화)
2. 그래도 안 되면 **🟢 실시간 감시** → `🩺 소리·알림 상태 진단`
   - 오디오가 `suspended`면 클릭이 인식되지 않은 것입니다
   - `none`이면 아직 컨텍스트가 없습니다 → `🔔 소리·알림 활성화` 클릭
3. 음량이 0은 아닌지, 조용한 시간대에 걸리지 않았는지 확인하세요

### 🖥 윈도우 알림이 안 뜹니다

**🩺 진단**의 `데스크톱 알림` 항목을 보세요.

- `denied` → 주소창 **자물쇠 아이콘** → 알림 → 허용으로 변경
- `default` → `🔔 소리·알림 활성화` 를 눌러 권한을 요청
- `granted` 인데도 안 보이면 → Windows **집중 지원(방해 금지)** 설정을 확인하세요

### 🔘 경보 카드가 안 사라지거나 ✕ 가 안 눌립니다

v23에서 해결됐습니다. 카드는 JS 클로저·타이머를 전혀 쓰지 않습니다
(✕ = 인라인 속성 · 자동 소멸 = CSS 애니메이션 · 이동 = 순수 링크).
그래도 반응이 없으면 F5로 새로고침하세요.

### 🛰 경보 카드가 아예 안 보입니다

브라우저 확장이나 엄격한 CSP가 주입을 막았을 수 있습니다.
**🟢 실시간 감시** → `📋 상단 배너(폴백)` 을 켜면 네이티브 위젯으로 대신 표시됩니다.

### 📨 알림이 안 나갑니다

**🩺 진단** → `🔌 연결 테스트` → `📨 알림` 을 눌러 SMTP/Slack을 각각 확인하세요.
그다음 `📤 발송 감사 로그` 에서 실패 사유를 봅니다.

흔한 원인:
- 자동 발송 토글이 꺼져 있음 (기본 OFF)
- 이중 임계값 모드에서 **검토 등급은 Email이 안 나갑니다** (설계상 정상)
- 받는 사람이 비어 있음

### 🔒 잠금이 안 풀립니다

- 15분이 지나면 자동 해제됩니다
- 급하면 `🔓 잠금 무시하고 내가 검토` 를 누르세요

### 🔌 두 탭에서 같은 포트 배지가 보입니다

포트 충돌입니다. 둘 중 하나는 실제로 안 떠 있습니다.
`dashboard.py` = 8501, `ops_dashboard.py` = 8502 로 분리해 실행하세요.

### 🌐 화면 문구가 이상합니다

`i18n_data.py` 가 없으면 한국어 폴백으로 동작합니다.
**🩺 진단** → 모듈 버전에서 `i18n_data=O/X` 로 확인하세요.

---

## 9. 용어집

| 용어 | 의미 |
|---|---|
| **정탐 (TP)** | 실제 사기가 맞았다 |
| **오탐 (FP)** | 정상 거래인데 알림이 갔다 |
| **미탐 (FN)** | 알림이 안 갔는데 사기였다 |
| **오탐률** | 오탐 / (정탐 + 오탐) — 보류·미탐은 분모에서 제외 |
| **SLA** | 알림 발생 후 판정까지 약속한 시간 (기본 30분) |
| **등급(tier)** | 발송 등급: `none` / `review`(Slack만) / `confirm`(Slack+Email) |
| **잠금(claim)** | "내가 검토 중"을 다른 담당자에게 알리는 협조적 표시 |
| **임시저장(draft)** | 판정 중이던 입력을 DB에 보관 (새로고침 대비) |
| **워처(watcher)** | `inbox/` 를 감시하며 자동 탐지하는 무인 프로세스 |
| **분석 캐시** | 탐지 시점의 LLM 리포트·확률분포를 DB에 남긴 것 |
| **경보 등급** | 울릴 범위: `확정만` / `검토+확정` / `전체` (ops_alert) |
| **발송 등급** | 보낼 채널: `none`/`review`/`single`/`confirm` (ops_dispatch) |
| **드리프트** | 탐지 당시 모델과 현재 모델의 판정 차이 |
| **RAG** | 사기 유형·대응 매뉴얼 문서를 LLM에 함께 넣는 기법 |

### 사기 유형 코드

| 코드 | 이름 | 위험도 |
|---|---|---|
| a | 원거리 즉시이체 (계정탈취) | HIGH |
| b | 저신용층 표적 계좌탈취 | HIGH |
| c | 악성앱 정보탈취 → ATM 출금 | HIGH |
| d | 약신호 미분류 (조사 필요) | MEDIUM |
| e | 원격제어 대량입금 → ATM 전액인출 | HIGH |
| f | 위장 최종인출 (자금세탁 의심) | HIGH |
| g | 대량입금 인출 중간단계 | MEDIUM |
| h | 휴면계좌 재개 → 잔액부족 실패 | MEDIUM |
| i | 고액 거래 | — |
| j | 대포통장 | — |
| k | 오픈뱅킹 재사용 | — |
| l | 고령층 표적 | — |
| **m** | **정상 거래** | — |

---

## 10. 파일·모듈 지도

```
QAQC_streamlit/
├── ops_dashboard.py              ← 이 앱 (관제)
├── dashboard.py                  ← 분석용 대시보드
├── watcher.py                    ← 무인 감시 프로세스
├── fds_results.db                ← 모든 데이터 (SQLite)
├── inbox/                        ← 워처가 감시하는 폴더
├── models/                       ← 모델 파일
│
└── pipeline/
    ├── ops_ui.py            테마 · i18n · CSS · 헤더 배지 · 임계값 대조표
    ├── ops_sidebar.py       사이드바 전체
    ├── ops_guide.py         첫 실행 온보딩
    ├── ops_queries.py       알림 큐 · 라이브 피드 조회
    ├── review_store.py      판정 기록 · 🔒잠금 · 💾임시저장
    ├── ops_shift.py         ⏱SLA · 🔄교대 인수인계
    ├── ops_dispatch.py      자동 발송 · 감사 로그 · 연결 테스트
    ├── ops_alert.py         경보(배너·소리·데스크톱 알림)
    ├── ops_agent.py         챗봇 관제 액션 9종
    ├── ops_recheck.py       재검증 · 드리프트
    ├── analysis_store.py    분석 캐시
    ├── watcher_panel.py     워처 제어 패널
    │
    ├── detect_workbench.py  ★ 탐지 입력 6종·액션바 · 직접입력 계약 ·
    │                          🖊프롬프트/📚RAG 편집기 (dashboard.py와 공용)
    ├── detect_ui.py         ★ 게이지·확률·유형카드·규칙 (dashboard.py와 공용)
    ├── asset_registry.py    ★ 모델·데이터셋 탐색 (공용)
    ├── detect_io.py         분류기 로딩 · 탐지 저장
    ├── llm_analyzer.py      LLM 분석 3단계
    ├── chat_agent.py        AI 어시스턴트
    ├── rag_searcher.py      RAG 검색
    ├── rule_checker.py      규칙 적합도
    ├── notifier.py          Slack · Email 발송
    └── pii_masker.py        개인정보 마스킹
```

### DB 테이블

| 테이블 | 내용 |
|---|---|
| `transactions` | 원장 (불변) |
| `detections` | 탐지 결과 |
| `notified` | 발송 기록 |
| `analysis_cache` | 탐지 시점 LLM 리포트 |
| `alert_review` | **판정 기록** (번복도 지우지 않고 쌓임) |
| `alert_claim` | 🔒 잠금 (TTL 15분) |
| `review_draft` | 💾 임시저장 (사람별) |
| `shift_handover` | 🔄 인수인계 메모 + 스냅샷 |
| `watcher_status` | 워처 하트비트 |

---

## 부록 — 환경변수 (`.env`)

사이드바 입력값이 **우선**하며, 비어 있을 때만 `.env`를 씁니다.

```ini
# LLM
USE_LLM_PROVIDER=local
LLAMA_CPP_URL=http://localhost:8080
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
MOONSHOT_API_KEY=
CUSTOM_LLM_URL=

# 알림
SLACK_WEBHOOK_URL=
SMTP_USER=
SMTP_PASS=
FDS_NOTIFY_EMAIL=

# 경로·이름
FDS_DB_PATH=fds_results.db
FDS_LOG_PATH=watcher.log
FDS_MODEL_DIR=models/
FDS_REVIEWER=          # 검토자 기본 이름
```

---

## 11. 이번 개선분 (v29 → v36)

실무자 사용성 점검에서 나온 항목을 순서대로 반영한 결과입니다.
**확정 요구사항 두 가지는 그대로 유지**했습니다 — 첫 화면은 `🧠 AI 분석`이고,
워처의 1차 임계값은 `0.005` 입니다.

### 11.1 새로 생긴 것

| 기능 | 위치 | 왜 |
|---|---|---|
| **임계값 정밀 입력** | ⚙ 임계값 튜닝 | 슬라이더는 0.01 단위라 **0.005를 아예 입력할 수 없었습니다.** 현재 운영값을 다시 적용하는 것조차 불가능했고, 무심코 저장하면 소리 없이 2배가 됐습니다. 이제 오른쪽 `정확한 값` 칸이 실제 적용값입니다 |
| **임계값 적용 확인 카드** | ⚙ 임계값 튜닝 | 저장 = 무인 워처의 경보 기준 즉시 변경인데 클릭 한 번이었습니다. 이제 현재→변경 비교표, 알림/놓친사기 증감, 신뢰불가 구간 경고, **`watcher_config.json`에 기록된 결정 근거**를 보여준 뒤 한 번 더 묻습니다 |
| **발송 전 확인 카드** | 발송 버튼 6곳 전부 | 외부로 나간 것은 회수할 수 없습니다. 수신처·마스킹 레벨·본문 미리보기를 보여준 뒤 발송합니다 |
| **미탐(FN) 등록** | 📉 오탐 분석 | 트리아지 큐는 *알림이 나간 건*만 담아, 진짜 미탐을 기록할 창구가 없었습니다(집계의 미탐 칸이 늘 0). 거래 ID로 직접 등록하며, 원장에 있으면 "이 거래는 0.0031 < 임계값 0.005라 알림이 안 갔습니다"까지 알려줍니다 |
| **경보 마스터 스위치** | 사이드바 🛡 관제 설정 | "알람 어떻게 꺼요?"의 답이 탭 안쪽 접힌 패널이면 아무도 못 찾고, 결국 스피커를 끕니다. 켜기/끄기와 현재 등급만 사이드바로 올렸고 세부 설정은 그대로 실시간 감시 탭에 있습니다 |
| **화면 배치 비교 (실험)** | 🩺 진단 맨 아래 | 확정안(AI 분석 우선)과 관제 흐름 우선 배치를 눈으로 비교합니다. **세션 한정** — 새로고침하면 확정안으로 돌아오고 파일에 아무것도 쓰지 않습니다 |

### 11.2 자리를 옮긴 것

- **🖊 프롬프트 · 📚 RAG 편집기** : AI 분석 탭 맨 위 → **사이드바 아래쪽**
  설정 도구가 작업대의 첫 화면을 차지해, 탭을 여는 이유(탐지·분석)가 스크롤 아래로 밀려 있었습니다.
  저장해 둔 프롬프트는 그대로 유지됩니다(위젯 key 불변).
- **경보 켜기/끄기** : 실시간 감시 탭 → 사이드바 (세부 설정은 이동하지 않음)

### 11.3 표시 언어 (한국어 · English · 日本語 · 中文)

사이드바 `⚙ 고급 설정 → 🎨 표시`에서 바꿉니다. 이전에는 토글만 있고 화면 절반이
한국어로 남아 뒤죽박죽이었는데, 이번에 전 화면을 4개 국어로 채웠습니다.

**의도적으로 번역하지 않은 것**

| 대상 | 이유 |
|---|---|
| 인수인계서 **문서 본문** (`.md` 저장물) | 저장·배포되어 조직에 남는 산출물입니다. 화면 언어를 따라가면 "영어로 보다가 저장했더니 영문 인계서"가 됩니다 |
| 계좌 이력 **피처 이름** (최근 1개월 최대 거래액 등) | 화면 장식이 아니라 **모델 피처 사전**입니다. `model_meta.json`·임계값 리포트·이 설명서가 같은 이름을 씁니다. 화면만 바꾸면 리포트와 대조가 불가능해집니다 |
| Streamlit 버전 경고 (부팅 실패 화면) | 번역 함수가 만들어지기 **전**에 뜨는 하드 스톱입니다 |
| `log.*` 개발자 로그 | 사용자 화면이 아닙니다 |

> `dashboard.py`(분석용)는 이번 작업에서 **화면이 한 글자도 바뀌지 않았습니다.**
> 공유 모듈(`watcher_panel` · `detect_workbench`)은 언어 인자의 기본값이 한국어라,
> 인자를 넘기지 않는 저쪽 호출은 그대로 동작합니다.

### 11.4 검증 방법

`streamlit.testing.v1.AppTest`로 실제 런타임(Streamlit 1.58 / Python 3.11)에서
4개 국어 × 주요 화면 상태를 렌더해 **예외 0건**과 **번역 키 누출 0건**을 확인했습니다.
AI 분석 탭은 확정된 첫 화면이라, 번역 전후 한국어 렌더링 561행을 스냅샷으로 비교해
**문구 차이 0**(차이는 전부 경과시간·폴링 카운터 같은 런타임 값)을 확인했습니다.

### 11.5 아직 남은 것 (다음 과제)

1. ~~**기대비용 곡선이 미탐을 쓰지 않습니다.**~~ → **§12.1 에서 처리했습니다.**
   (원문) 등록한 FN은 판정 이력·교대 요약·재학습
   라벨에는 들어가지만 곡선은 여전히 정탐/오탐만 씁니다. 곡선에 넣으려면 비용 모델을
   함께 바꿔야 하는데, 그것은 현재 `0.005`를 정한 `tools/threshold_report.py`의 계산과
   충돌할 수 있어 분리해 두었습니다.
2. **피처 사전 다국어화** — 리포트·메타·문서까지 함께 가야 하는 별도 과제입니다.
   → **보류하기로 했습니다(2026-08-10).** 자세한 이유는 §12.4.
3. ~~**탭 8개의 밀도**~~ → **§12.2 에서 압축 라벨로 처리했습니다.**

---

## 12. 미탐 반영 곡선 · 압축 탭 (v37)

§11.5 에 남겨둔 과제 1·3 번을 처리한 결과입니다.

### 12.1 🔴 미탐을 반영한 두 번째 곡선 — ⚙ 임계값 튜닝

`📉 오탐 분석`에서 등록한 미탐(FN)을 기대비용 곡선에 넣을 수 있습니다.
토글 **`🔴 등록된 미탐을 곡선에 반영`** (기본 꺼짐).

**기존 곡선을 덮어쓰지 않습니다.** 별도 함수(`oq.threshold_whatif_fn`)로 계산해
빨간 점선으로 **겹쳐 그리고**, 추천 슬라이더는 계속 기존 곡선을 따릅니다.
숫자를 조용히 바꿔치기하면 "어제 본 추천치와 오늘이 다른데 왜인지 모르는" 상태가
되는데, 임계값 도구가 절대 만들면 안 되는 상태이기 때문입니다.

이 프로젝트에는 임계값 숫자가 이미 두 개 있습니다.

| | 출처 | 미탐을 보는가 |
|---|---|---|
| ① `tools/threshold_report.py` | 검증셋(정답 라벨) — **현재 0.005 가 여기서 나왔습니다** | 전부 봄 |
| ② `⚙ 임계값 튜닝` 기존 곡선 | 운영 판정(정탐/오탐) | **구조적으로 못 봄** |
| ③ 미탐 반영 곡선 (신규) | 운영 판정 + 등록된 미탐 | 등록된 것만 |

③은 ①·② 어느 쪽도 건드리지 않습니다. ①은 애초에 다른 파일에서 parquet으로
계산하고, ②는 코드가 한 줄도 바뀌지 않았습니다(미탐이 0건이면 두 곡선은 **완전히 동일**합니다).

#### 곡선을 읽는 법 — 두 가지를 헷갈리면 안 됩니다

1. **높이가 올라간 것은 정상입니다.** 기존 곡선이 아예 보지 못했던 실제 손실이
   이제 계산에 들어왔기 때문입니다. "미탐을 넣었더니 비용이 늘었다"는 버그가
   아니라, 원래 있던 손실이 이제 보이는 것입니다.
2. **쓸모 있는 것은 최소점의 위치입니다.** 낮은 임계값에서 회수되는 사기가
   계산에 들어오므로 최소비용 지점이 **왼쪽(더 낮은 쪽)으로** 움직입니다.
   임계값을 내릴 근거는 바로 이 이동입니다.

> 🚨 **비대칭 주의** — 미탐 데이터는 임계값을 내렸을 때의 *이득만* 알려줍니다.
> 내려서 새로 올라올 **오탐이 몇 건일지는 여전히 관측된 적이 없습니다.**
> 이동한 거리를 그대로 믿지 말고 **방향의 근거로만** 쓰세요.

원장에 점수가 없는 미탐(워처가 처리한 적 없는 거래)은 곡선에 올릴 수 없어
`⚠️ 미탐 N건은 원장에 점수가 없어 곡선에 반영되지 않았습니다` 로 따로 알려줍니다.

### 12.2 🗜 압축 탭 라벨 — 🩺 진단

탭 8개의 라벨을 짧게 줄입니다. **순서와 내용은 그대로**이고 첫 탭도 그대로입니다.

측정해보니 기본 라벨의 탭바 폭은 이렇습니다.

| 언어 | 기본 | 압축 | |
|---|---:|---:|---|
| 한국어 | 1,096px | **728px** | 34% 감소 |
| English | 1,072px | 720px | 33% 감소 |
| 日本語 | 1,048px | 760px | 28% 감소 |
| 中文 | 944px | 680px | 28% 감소 |

1366px 노트북에서 사이드바(약 336px)를 빼면 본문은 약 **1,030px** 입니다.
즉 한국어·영어·일본어는 기본 라벨로 **넘치고**(마지막 탭이 잘림), 압축하면
네 언어 모두 들어옵니다. 관제실 모니터가 넓으면 켤 필요가 없어 **기본은 꺼짐**입니다.

진단 탭 아래쪽에 현재 폭이 `현재 탭바 폭 약 728px (압축)` 처럼 표시되므로,
자기 화면에서 켤지 말지 바로 판단할 수 있습니다.

> 배치(순서) 스위치와 **분리된 토글**입니다 — 확정된 순서는 그대로 두고
> 폭만 줄이고 싶은 경우가 대부분이기 때문입니다.
> 환경변수 `FDS_OPS_TAB_COMPACT=1` 로 처음부터 켤 수도 있습니다.

### 12.3 검증

- 미탐 곡선 계산을 손계산과 대조(τ별 정탐·오탐·놓친사기·기대비용) — 일치
- **미탐 0건이면 두 곡선이 완전히 동일** — 기존 동작 회귀 없음
- 최소점 이동 확인: 표본 200건 + 미탐 25건에서 **τ 0.18 → 0.10** (왼쪽 이동)
- 4개 국어 × 토글 on/off × 배치안 조합 렌더 — 예외 0
- 라벨 집합이 바뀌어도(언어·압축 전환) 탭 선택이 깨지지 않음

### 12.4 남은 과제 — 피처 사전 다국어화 (보류)

영어·일본어·중국어 화면에 남아 있는 한국어는 **계좌 이력 피처 이름 5개**뿐입니다
(`최근 1개월 최대 거래액` 등). 이것만 남긴 것은 빠뜨려서가 아닙니다.

| 같은 이름을 쓰는 곳 | 화면만 번역하면 |
|---|---|
| `model_meta.json` (모델 피처 사전) | 모델과 화면이 다른 이름으로 같은 피처를 부릅니다 |
| 임계값 리포트 (`tools/threshold_report.py` 산출물) | **리포트와 화면을 대조할 수 없습니다** |
| 이 설명서 | 문서를 보며 화면을 찾을 수 없습니다 |

즉 화면 번역 작업이 아니라 **사전 마이그레이션**이고, 넷을 한 번에 옮겨야 합니다.
**2026-08-10 자로 보류하기로 했습니다** — 현재 이 5개 때문에 막히는 작업이 없고,
섣불리 화면만 바꾸면 대조가 깨져 오히려 나빠지기 때문입니다.

> 추적 위치: `OPS_BACKLOG.md` **B1**. 손댈 때 무엇을 함께 옮겨야 하는지가
> `pipeline/detect_io.py`의 `ACCOUNT_HISTORY_FIELDS` 위에 주석으로 적혀 있습니다.

---

### 12.5 전체 검증 (v37 기준)

```
selftest_all              12/12 통과 (79.0초)
전체 컴파일 (3.11.15)     60파일 · 실패 0
i18n                      571키 × 4개국어 · 누락 0
확정 요구사항             첫 탭 = 🧠 AI 분석 · th_review = 0.005 불변
```

코드를 손대기 전후로 **`python -m pipeline.selftest_all`** 을 돌리면 됩니다
(전체 12종 약 80초 · `--fast` 는 UI·원본대조를 빼고 6초).

---

## 13. 키보드 단축키 · AI 액션 확장 (v39)

`dashboard.py`(분석용)에는 있는데 관제 콘솔에는 없던 두 가지를 채웠습니다.

| | 이전 | v39 |
|---|---|---|
| 키보드 단축키 | **0개** (기능 자체가 없음) | **12종** + `?` 전체 목록 창 |
| AI 어시스턴트 액션 | 9종 | **25종** (분석용 21종보다 많습니다) |
| 퀵프롬프트 | 없음 (예시 문구만) | 버튼 4개 |
| 에이전트 자가진단 | 없음 | 등록 수 · 파서 검증 · 액션 직접 실행 |

### 13.1 단축키 — 사용법은 §3

키 배치는 **`dashboard.py`와 일부러 같게** 했습니다. 두 앱을 오가는 담당자의
근육기억이 그대로 통하는 편이 낫기 때문입니다. `R`(지금 새로고침)과
`A`(자동 새로고침)만 관제 전용으로 추가했습니다 — 저쪽에는 없는 개념입니다.

설계상 신경 쓴 곳이 하나 있습니다. 단축키 처리를 화면 **맨 끝**에 둡니다.
Streamlit은 어떤 런에서 그려지지 않은 위젯의 상태를 버리기 때문에, 단축키가
화면 앞쪽에 있으면 키를 누르는 순간 뒤쪽 탭의 위젯(트리아지 필터·로그
검색어 등)이 통째로 초기화됩니다. 탭 이동·사이드바처럼 파이썬 상태가 필요
없는 동작은 아예 브라우저에서 직접 처리해 재실행조차 걸지 않습니다.

### 13.2 AI 액션 9 → 25종

늘린 갈래는 전부 **되돌릴 수 있는 표시 설정**(기간·필터·정렬·검색어·
새로고침·집계 기준)이거나, **밖으로 아무것도 내보내지 않는 실행**
(AI 분석·일괄 분석)입니다. 발송·판정·워처 제어는 이전과 똑같이 막혀
있습니다 — 자세한 목록과 이유는 §4의 `🤖 AI 어시스턴트`에 있습니다.

경보 등급 임계값(`th_review`/`th_confirm`) 저장은 **새로 막은 축**입니다.
표면상 화면 설정이라 넣을 뻔했는데, 이 값은 `watcher_config.json`에 저장되고
그 파일은 핫 리로드됩니다 — 즉 저장하는 순간 재시작 없이 무인 워처의 경보
기준이 바뀝니다. 실질적으로 워처 제어라 사람이 비용곡선(§12.1)을 보고
직접 누르는 쪽으로 남겼습니다.

### 13.3 🐛 함께 고친 것 — 정렬 지시가 먹지 않던 문제

*"위험점수 순으로 정렬해줘"* 가 **동작하지 않았습니다.** 챗봇은
"정렬을 바꿨습니다"라고 답하는데 화면은 그대로였습니다.

원인은 값의 형식이었습니다. 액션이 화면 표시 문구(`"점수순"`)를 넣었는데
정렬 위젯이 실제로 받는 값은 `age`/`score` 였고, 그 위젯 바로 앞에는
"목록에 없는 값이면 버린다"는 구버전 값 정리 코드가 있습니다. 그래서 지시가
**매번 조용히 버려졌습니다** — 실패했다는 표시조차 없었던 것이 문제의
핵심입니다. 값을 위젯이 받는 형식으로 맞추고, 같은 방식으로 죽지 않도록
25종 전체에 대해 "상태가 실제로 바뀌었는가"를 검사하는 테스트를 넣었습니다.

### 13.4 검증 (v39 기준)

```
selftest_all              12/12 통과 (167.2초)
selftest_agent            액션 25종 · 파서 왕복 전수 · 위험 액션 차단
selftest_ui (AppTest)     히든 버튼 8개 실제 클릭 · 예외 0
컴파일                    Python 3.11.15 (배포 환경과 동일)
```

새 테스트는 값이 바뀌었는지와 **예외가 0인지를 항상 함께** 봅니다. 값만
검사하면 Streamlit이 예외를 던져 화면에 빨간 박스가 떠도 테스트는 통과해
버리는데, 이번 작업 중 실제로 그렇게 두 번 놓쳤습니다.

> ⚠️ 알려진 제약 — 화면 언어를 한국어 외로 바꾼 뒤 `AppTest`를 **다시**
> 실행하면 하네스가 라디오 옵션 계산에서 죽습니다(`batch_src`). 이번 작업과
> 무관한 기존 현상이며(단축키를 들어낸 사본·사이드바 언어 라디오에서도 동일),
> 실제 브라우저 동작에는 영향이 없습니다. 다만 이 때문에 **비한국어 상태의
> 연속 렌더는 자동 검증이 되지 않습니다.**
> 추적 위치: `OPS_BACKLOG.md` **B2** (원인·재현 경로·선택지 정리돼 있음).

*최종 갱신: 2026-08-10 · `ops_dashboard v39`*
