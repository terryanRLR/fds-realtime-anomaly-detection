# FDS 대시보드 — 파이프라인 모식도

> 발표용 3종: **① 전체 상세판** · **② 한 슬라이드 간략판** · **③ 이전 대비 변화표**
> **v22 갱신** — 🆕 **관제 콘솔(판정 루프)**: 오탐 피드백 · 동시 판정 잠금 · SLA · 교대 인수인계
> · AI 작업대 전면 배치(범위 추출·액션바·일괄 분석) · 레이더 경보 카드 · 챗봇 관제 액션 9종
> · v18 기반: 실시간 폴더 감시 워처 · 헤드리스 판정 코어 · 운영 계층 · AI 에이전트 21액션
> 폭 88자 기준(16:9 슬라이드 안전폭). 고정폭 글꼴(D2Coding·JetBrains Mono·Consolas) 권장.

---

## ① 전체 상세판

```
╔══════════════════════════════════════════════════════════════════════════════════════╗
║  FDS 실시간 이상거래 탐지 시스템                                                        ║
║  LightGBM 13-class · 58 features · macro F1 0.6138 (검증 재현 완료)                    ║
║  대시보드(수동) + 워처(무인 상시) — 판정 엔진 1개를 공유                                  ║
╚══════════════════════════════════════════════════════════════════════════════════════╝

┌─ ① 입력 계층 ────────────────────────────────────────────────────────────────────────┐
│                                                                                      │
│  ── 사람이 여는 경로 (대시보드) ────────────────────────────────────────────────────   │
│   ✏️ 직접입력      📄 test.csv      📊 train.csv      🧪 합성생성      📁 폴더배치        │
│   (수동 폼)       (임의추출)       (정답 포함)       (분포 기반)      (일괄 스트림)       │
│                                                                                      │
│  ── 🆕 사람 없이 도는 경로 (워처) ──────────────────────────────────────────────────   │
│   👁 inbox/ 실시간 감시 — 5초 폴링 · 신규 파일 & 행 추가(append) 모두 자동 처리          │
│      · 파일별 커서(경로·크기·처리행수·헤더해시)로 재처리·누락 방지                       │
│      · mtime 3초 + 크기 불변 → 쓰는 중인 반쪽 파일 회피                                 │
│      · watchdog 대신 폴링 — SMB 공유에서 이벤트를 놓치지 않기 위해                       │
│        └───────────────┴────────────────┴────────────────┴────────────────┘          │
│                                        │                                             │
│                     📦 Parquet 데이터셋 (X_tr / X_va · 120,000행) ─────┤              │
└────────────────────────────────────────┼─────────────────────────────────────────────┘
                                         ▼
┌─ ② 전처리 계층 ──────────────────────────────────────────────────────────────────────┐
│                                                                                      │
│   ┌──────────────────────┐    행의 "컬럼 구성"으로 판정 (값 타입 아님)                   │
│   │ 🔀 분류기 선택 관문    │───┬─ raw        → 원본 컬럼 보유                            │
│   │  _resolve_classifier │   ├─ engineered → 58피처 커버리지 90%↑                      │
│   └──────────────────────┘   └─ unknown    → 안전 폴백                                │
│              │                                                                       │
│              ▼                                                                       │
│   ┌────────────────────────────────────────────────────────────────┐                 │
│   │ 🧮 Preprocessor — 원본 → 58피처 결정론적 변환                     │                 │
│   │    · 원본 통과 47개  +  파생 공식 11개 (100% 검증)                │                 │
│   │    · |amt|/한도 · 시각 · 시도코드 · 조합플래그 …                   │                 │
│   │    · ⚠️ NaN 보존 (LightGBM native) — 채우면 F1 −0.0068          │                 │
│   └────────────────────────────────────────────────────────────────┘                 │
│              ▲                                                                       │
│   📁 bundle_io.safe_load — pickle → joblib 폴백 (배포 번들 호환)                        │
└──────────────────────────────────────┬───────────────────────────────────────────────┘
                                       ▼
┌─ ③ 추론 계층 ────────────────────────────────────────────────────────────────────────┐
│                                                                                      │
│   🤖 LightGBM 13-class (a~m)                                                          │
│      입력 58피처  →  P(a) … P(m)                                                      │
│      · 예측 유형 = argmax          · 위험점수 = 1 − P(m)                               │
│                                                                                      │
│   ┌── 📮 이중 임계값 구간 판정 (dual threshold) ─────────────────────────────────┐     │
│   │                                                                            │     │
│   │   위험점수  0.0 ────── 1차 T₁(0.10) ──────────── 2차 T₂(0.90) ────── 1.0   │     │
│   │             │  🟢 none  │       🟡 review          │   🔴 confirm           │     │
│   │             │ 발송 생략  │   Slack만 · 검토요청       │ Slack+Email · 확정통보  │     │
│   │             │           │                          │                       │     │
│   │   💰 기대비용 기반 결정 (X_va 23,860행 · 사기 258건 1.08%)                    │     │
│   │      기대비용 = 미탐×1,700만원(보이스피싱 평균) + 오탐×5만원(대응비)            │     │
│   │      T₁ 0.10→0.005 : 7.48억 → 4.62억 (−2.86억) · 미탐 44→27건                │     │
│   │      순수 비용최소는 임계값을 0으로 밀어 모델 붕괴 → Macro-F1≥0.6 제약 하 최적화 │     │
│   │      견고성: 오탐 단가가 425만원(가정의 85배)이어야 결론이 뒤집힘               │     │
│   │      → tools/threshold_report.py 로 언제든 재측정                            │     │
│   │                                                                            │     │
│   │   · 미탐 안전망: 점수가 낮아도 **예측 유형이 사기면 review로 승격**              │     │
│   │   · 등급별 머리말·제목 자동 전환 (검토 요청 ↔ 즉시 대응)                        │     │
│   │   · T₂ < T₁ 오설정 시 T₂ = max(T₁,T₂) 자동 보정                              │     │
│   │   · 대시보드에서 조정 → **재시작 없이 5초 내 워처에 반영**                       │     │
│   └────────────────────────────────────────────────────────────────────────────┘     │
│                                                                                      │
│   🟢 none ────────────────────────────────────────► 정상 처리 → SQLite 기록           │
│   🟡 review / 🔴 confirm ──┐                                                          │
└────────────────────────────┼─────────────────────────────────────────────────────────┘
                             ▼
┌─ ④ 설명 계층 (환각 방지의 핵심) ──────────────────────────────────────────────────────┐
│                                                                                      │
│   ┌─────────────────────────────┐        ┌──────────────────────────────┐            │
│   │ 📋 RuleChecker              │        │ 📚 RAG (Chroma + KR-SBERT)   │            │
│   │  13유형 × 56지표             │        │  docs/*.md · 122 청크         │            │
│   │  · 충족/미충족 + 실제 값      │        │  · 유형별 정의·지표·조치       │            │
│   │  · 적합도 지수(기준선 정규화)  │        │  · 대시보드에서 편집 가능       │            │
│   │  · Top-3 81.1% (무작위 3.2배)│        │  · 편집 시 자동 재임베딩        │            │
│   └──────────────┬──────────────┘        └───────────────┬──────────────┘            │
│                  │                                       │                           │
│   ⚠️ 모델 ↔ 규칙 불일치(1.3배↑) → 수동 검토 대기열로 승격                                │
│   ※ 규칙은 "어느 유형인가"만 판정. "사기냐 정상이냐"는 모델의 몫                           │
└──────────────────┴───────────────────────────────────────┴───────────────────────────┘
                                       ▼
╔══════════════════════════════════════════════════════════════════════════════════════╗
║  🔒 ⑤ PII 마스킹 게이트 — 외부로 나가는 모든 데이터의 단일 통로                            ║
║                                                                                      ║
║     off  →  basic (이름·식별번호)  →  standard (+IP·위치·계좌)  →  strict (+생년·시간)   ║
║     · 컬럼명 기반 + 정규식 이중 안전장치                                                 ║
║     · 🚫 로컬 LLM + 마스킹 생략 모드 = 외부 전송 전면 차단 (음성 입력도 락 승계)            ║
╚══════════════════════════════════════════════════════════════════════════════════════╝
                                       ▼
┌─ ⑥ 생성 계층 ────────────────────────────────────────────────────────────────────────┐
│                                                                                      │
│   🧠 LLM (llama.cpp 로컬 · Claude · GPT · DeepSeek — 교체 가능)                        │
│                                                                                      │
│     1단계 ▸ 분석 리포트   판정근거 · 이상패턴 · 즉시/단기/장기 조치                       │
│     2단계 ▸ Slack 알림    요약 1줄 + 핵심 지표                                          │
│     3단계 ▸ 이메일 본문   담당자용 상세 + HTML                                          │
│                                                                                      │
│   🛡 3중 폴백 방어   ① 헤더 앞 잡담 제거  ② 반복·붕괴 출력 감지  ③ 템플릿 폴백            │
│                     → LLM이 죽어도 보고서는 항상 나온다                                 │
└──────────────────────────────────────┬───────────────────────────────────────────────┘
                                       ▼
┌─ ⑦ 전달 계층 — 기술 스택 ────────────────────────────────────────────────────────────┐
│                                                                                      │
│  🟡 review 등급                          🔴 confirm 등급                              │
│      └─► Slack만                             └─► Slack + Email 동시                   │
│                                                                                      │
│  ┌──────────────────────────────┐   ┌──────────────────────────────────────────┐     │
│  │ 💬 Slack Incoming Webhook    │   │ 📧 SMTP (Python stdlib `smtplib`)        │     │
│  │                              │   │                                          │     │
│  │  requests.post(HTTPS)        │   │   465 → SMTP_SSL (암시적 TLS)             │     │
│  │  JSON {"text": …}            │   │   587 → SMTP + STARTTLS                  │     │
│  │  timeout 10s                 │   │   ↳ 465 차단 시 587 자동 재시도            │     │
│  │  ⚙ Block Kit 미사용          │   │                                          │     │
│  │   (mrkdwn 텍스트 + 유니코드   │   │   `email.message.EmailMessage`           │     │
│  │    막대그래프로 시각화)        │   │    multipart/alternative                 │     │
│  │                              │   │    ├ text/plain  (폴백)                  │     │
│  │  🔗 웹훅 URL은 마스킹 저장    │   │    ├ text/html   (본문)                  │     │
│  │   (.../services/*** 표기)    │   │    └ 첨부: HTML 리포트 · 시각화           │     │
│  └──────────────────────────────┘   └──────────────────────────────────────────┘     │
│                                                                                      │
│  📊 시각화 — 외부 이미지 서버 없이 순수 텍스트·HTML로 생성 (notify_visuals)              │
│     위험 게이지 ████████░░ 0.87 · 유형 분포 스파크라인 · 확률 막대                       │
│     → 이메일 클라이언트·Slack 어디서나 깨지지 않고, 이미지 차단 정책도 통과               │
│                                                                                      │
│  🗄 SQLite 사고 이력 (거래ID·유형·위험점수·발송등급·규칙충족)                             │
│  🖥 대시보드 실시간 표시 (탐지 결과 · 규칙 근거 · 확률 분포)                               │
└──────────────────────────────────────────────────────────────────────────────────────┘

┌─ 🆕 ⑧ 운영 계층 — 무인 운영을 지탱하는 장치 ─────────────────────────────────────────┐
│                                                                                      │
│   🧩 DetectService — 판정 코어 (Streamlit 무관)                                        │
│      대시보드 · 워처 · (향후) MCP 가 **같은 엔진 하나**를 호출                            │
│      → 로직이 두 벌이 되면 "대시보드는 정상인데 알림은 왔어요" 사고가 난다                 │
│                                                                                      │
│   🖥 워처 상태 패널 (대시보드 세션5 + 사이드바 배지)                                     │
│      🟢 정상 / 🔵 1회 실행 / 🔴 응답 없음 / ⚫ 미실행                                   │
│      폴링·처리행·이상거래·발송·오류 · 파일별 진행도 · watcher.log 꼬리                    │
│                                                                                      │
│   ⚙️ 설정 핫 리로드 (watcher_config.json)                                              │
│      임계값·마스킹·채널을 재시작 없이 변경 — 워처가 폴링마다 mtime 확인                    │
│                                                                                      │
│   🔌 시작·중지 버튼 — **기본 잠김** (.env FDS_ALLOW_WATCHER_CONTROL=1 로 해제)          │
│      공유 URL을 아는 누구나 프로세스를 켜고 끌 수 있게 되므로 명시적 해제를 요구            │
│      중지는 kill 이 아니라 신호 파일 → 처리 중인 행을 마치고 커서 저장 후 종료             │
│                                                                                      │
│   💀 데드맨 스위치 (Windows 작업 스케줄러 · 10분 간격)                                   │
│      하트비트 10분 이상 끊기면 Slack 경보 → 60분 쿨다운 → 복구 시 1회 알림                │
│      의도적 중지(note=stopped)는 조용히 넘어감 — 아니면 끌 때마다 알림이 와서 무시하게 됨   │
│      ※ 상주 데몬을 만들지 않은 이유: **감시자도 죽는다.** OS 스케줄러가 가장 안 죽는다     │
│                                                                                      │
│   🚨 안전 가드                                                                        │
│      · 모델 로드 실패 시 **기동 거부**(exit 2) — 더미 모드의 15% 랜덤 판정이 새벽에        │
│        Slack 으로 나가는 것을 원천 차단                                                 │
│      · LLM 연속 3회 실패 → 서킷 브레이커 5분 — 알림 자체는 폴백 양식으로 계속 발송         │
│      · 중복 실행 방지 락 · 같은 거래 24시간 재알림 억제                                   │
└──────────────────────────────────────────────────────────────────────────────────────┘

┌─ ⊕ 횡단 계층 (전 구간 적용) ──────────────────────────────────────────────────────────┐
│                                                                                      │
│   🤖 AI 에이전트   자연어로 대시보드 조작 — 액션 21종 화이트리스트                         │
│                   "임계값 0.7로 바꿔줘" · "고위험 예시 채워줘" · "일괄 분석 돌려줘"        │
│                   🎤 음성 입력 (로컬 faster-whisper / 클라우드 STT)                     │
│                                                                                      │
│   ✨ v18 — 화면만 보던 봇이 '사실'을 보게 됐다                                            │
│      매 턴 DB·설정에서 조회한 값을 컨텍스트에 주입 (워처 상태·오늘 탐지·임계값 실측표)        │
│      🩺 자가진단 체인 7단계 — "왜 알림이 안 와?" 에 원인을 순서대로 짚어 답                 │
│         ① 워처 생존 ② 파일 유입 ③ 임계값 ④ 발송 스위치 ⑤ 채널 설정 ⑥ LLM ⑦ 중복 억제      │
│      ⚠️ 사실 확인은 파이썬, 설명만 LLM — 진단을 LLM이 추론하면 그럴듯한 오답을 만든다        │
│      🔌 워처 제어 4종: 임계값 변경 · 중지(예약 재개) · 시작 · 파일 재처리                   │
│         중지·재처리는 패널 승인 카드를 거쳐야 실행 (자연어는 "꺼줘"와 "끄면?"을 헷갈린다)   │
│                                                                                      │
│   ┌── 🚦 승인 게이트 — 되돌릴 수 없는 액션에만 사람 확인 ──────────────────────────┐      │
│   │                                                                          │      │
│   │   가역 액션 15종 ─────────────────────────────► 즉시 실행                  │      │
│   │   (임계값·모델·화면·입력폼 …)                     되돌리면 그만               │      │
│   │                                                                          │      │
│   │   비가역 액션 2종 ──► 🚦 게이트 ON (기본) ──► 확인 카드 ──► 사람 승인 ──► 발송 │      │
│   │   request_send            │                  채널·수신자·제목                │      │
│   │   cancel_send             │                  마스킹 수준·본문 미리보기         │      │
│   │                           └─ 게이트 OFF ────────────────────► 즉시 발송      │      │
│   │                              (빠른 업무 선호 · 감사 로그 기록 · 경고 표시)      │      │
│   └──────────────────────────────────────────────────────────────────────────┘      │
│                                                                                      │
│   🌐 4개국어 (한·영·일·중) 739키   🎓 온보딩 도우미   ⌨ 단축키 11종                      │
│   📐 컴팩트 모드 (무스크롤 1화면)   🩺 자가진단 (번들·LLM·에이전트·음성)                    │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

---

## ② 한 슬라이드 간략판

```
   [ 거래 데이터 ]
         │  사람 경로 : ✏️직접입력 / 📄test.csv / 📊train.csv / 🧪합성생성 / 📁폴더배치
         │  🆕 무인 경로: 👁 inbox/ 실시간 감시 (5초 폴링 · 신규+append · 커서 추적)
         ▼
   ┌─────────────────┐
   │  전처리 계층      │   원본 → 58피처 결정론적 변환 (파생 11개 100% 검증)
   └────────┬────────┘
            ▼
   ┌─────────────────┐
   │  ML 추론         │   LightGBM 13-class · macro F1 0.6138
   └────────┬────────┘   위험점수 = 1 − P(정상)
            ▼
   ┌──────────────────────────────────┐
   │  📮 이중 임계값 구간 판정            │  기대비용 최적 (제약 F1≥0.6)
   │  0.0 ─ T₁ 0.005 ── T₂ 0.90 ── 1.0│  기대비용 4.62억 · 미탐 27건
   └───┬────────┬─────────────┬───────┘  유형이 사기면 점수 무관 review 승격
       │        │             │
       └────────┴─────────────┴──────────▶ 🗄 SQLite 이력 DB
                                            등급과 무관하게 **전 건 기록**(정상 포함)
                                            detections · transactions · notified
                                            watch_cursor · watcher_status (5개 테이블)
                                            ↑ 발송 이력    ↓ 상태 조회
                                            ⑧ 알림        ⑨ 상태 패널 · 데드맨
       ▼        ▼             ▼              ─────▶ SQLite 기록
   🟢 none   🟡 review    🔴 confirm
   자동발송     Slack만       Slack+Email
   생략        검토 요청       확정 통보
       │        └──────┬──────┘
       ▼               ▼
   ┌──────────────────────────────────┐
   │  설명 계층                         │   📋 규칙 체크리스트 (56지표 · 실제 값)
   │  "왜 이렇게 판정했는가"              │   📚 RAG 근거 문서 (122청크)
   └────────────────┬─────────────────┘   ⚠️ 모델↔규칙 불일치 → 검토 승격
                    ▼
   ╔══════════════════════════════════╗
   ║  🔒 PII 마스킹 게이트               ║   외부 전송 전 단일 통로
   ║  4단계 · 로컬모드면 전송 차단        ║
   ╚════════════════┬═════════════════╝
                    ▼
   ┌──────────────────────────────────┐
   │  LLM 생성 (3단계)                  │   분석 리포트 / Slack / 이메일
   │  🛡 3중 폴백 — 죽어도 보고서는 나옴  │   HTTP POST /v1/chat/completions
   └────────────────┬─────────────────┘   (llama.cpp 로컬 · Claude · GPT)
                    ▼
   ┌────────────────────────┬─────────────────────────────┐
   │ 💬 Slack Webhook       │ 📧 SMTP  (smtplib)          │
   │   requests.post HTTPS  │   465 SSL ↔ 587 STARTTLS    │
   │   JSON {"text": …}     │   multipart/alternative      │
   │   mrkdwn + 유니코드 차트 │   plain + HTML + 첨부        │
   └────────────────────────┴─────────────────────────────┘
                    ▼
   📊 시각화(텍스트·HTML 생성 · 외부 이미지 서버 없음)
   🗄 SQLite 이력   🖥 대시보드 실시간 표시

   ─────────────────────────────────────────────────────────────
   🆕 ⊕ 운영 계층
      🧩 DetectService — 대시보드·워처가 공유하는 판정 엔진 1개
      🖥 워처 상태 패널(🟢🔵🔴⚫)   ⚙️ 설정 핫 리로드(재시작 없이 5초)
      🔌 시작·중지(기본 잠김)        💀 데드맨 스위치(스케줄러 10분)
      🚨 모델 실패 시 기동 거부 · LLM 서킷 브레이커 · 중복 알림 억제
      🤖 에이전트 21액션 — 사실 주입 · 자가진단 7단계 · 워처 제어(승인 카드)
   ─────────────────────────────────────────────────────────────
   ⊕ 전 구간: 🤖 AI 에이전트(21액션·음성) · 🌐 4개국어 · 🎓 온보딩

   🤖 에이전트 발송 요청 ──► 🚦 승인 게이트 ──► 확인 카드 ──► 사람 승인 ──► 발송
                              (기본 ON · 토글로 해제 가능)

   ═════════════════════════════════════════════════════════════
   🆕 ⊕ 판정 루프 (v20 관제 콘솔 :8502) — 여기서 파이프라인이 '닫힌다'
   ═════════════════════════════════════════════════════════════
      알림 발송 ──► 🚨 트리아지 ──► 정탐/오탐/미탐/보류 ──► 📉 오탐 분석
                      │                    │                    │
              ⏱ SLA 경과시간         🔒 동시 판정 잠금        ⚙ 임계값 재계산
              (대기순 정렬)          💾 DB 임시저장           (실판정 기반 비용)
                      │              (새로고침 내성)                │
              🔄 교대 인수인계 ◄──────────┘                          ▼
              (요약 스냅샷 동봉)                            watcher_config
                                                          (재시작 없이 5초 반영)
   ─────────────────────────────────────────────────────────────
   ※ 지금까지 임계값 근거는 **검증셋(과거 정적 라벨)** 뿐이었다.
      이 루프가 생기면서 **운영 분포의 실제 판정**으로 다시 계산할 수 있게 됐다.
```

---

## ③ 이전 모식도 대비 — 무엇이 달라졌나

| 구간 | 이전 | 현재 | 왜 바뀌었나 |
|---|---|---|---|
| **탐지 모델** | Isolation Forest<br>(비지도 · 이상/정상 2분류) | LightGBM 13-class<br>(지도 · 유형까지 판정) | 담당자에게 필요한 건 "이상하다"가 아니라 **"어떤 수법인가"** — 대응 방법이 유형마다 다름 |
| **전처리** | (명시 없음) | 결정론적 58피처 변환<br>+ 분류기 선택 관문 | 입력 5경로가 서로 다른 형태 → **단일 관문**으로 통일. 파생 공식 11개 100% 검증 |
| **판정 근거** | 없음<br>(LLM이 알아서 서술) | 규칙 체크리스트 56지표<br>+ RAG 122청크 | LLM 환각의 원인은 **근거 부재**. 실제 값을 주면 인용하고, 없으면 지어냄 |
| **개인정보** | (게이트 없음) | PII 마스킹 4단계 게이트 | 외부 전송 경로를 **하나로 좁혀** 통제. 음성 입력도 같은 락 승계 |
| **LLM** | 단일 호출 | 3단계 생성 + 3중 폴백 | 용도별 최적 길이 확보 + **LLM이 죽어도 보고서는 나오게** |
| **발송 기준** | 단일 임계값<br>(이상/정상 2분기) | **이중 임계값 3구간**<br>none / review / confirm | 위험도에 따라 **채널과 문체를 이원화** — 검토 요청과 확정 통보를 구분해 알림 피로 감소 |
| **알림 경로** | MCP SMTP/Mail 서버<br>(Tool 호출 경유) | **SMTP 직결 + Slack Webhook**<br>`smtplib` · `requests` | 중간 서버 제거로 장애점 감소, 사내 방화벽 환경 대응. ⚠️ 현재 코드에 **MCP 미사용** |
| **운영** | (단방향 파이프라인) | AI 에이전트 17액션<br>+ 음성 입력 | 담당자가 **말로 대시보드를 운전** — 임계값·모델·데이터셋 전환 |
| **에이전트 안전** | (에이전트 없음) | **승인 게이트**<br>비가역 액션만 사람 확인 | LLM 오판의 비용이 액션마다 다르다 — 임계값은 되돌리면 되지만 **메일은 회수 불가** |
| **검증** | (없음) | `verify_bundle` 하니스 | 모델 교체 시 **30초 안에** 정상 연결 판정 |
| **실행 방식** | 대시보드에서 **사람이 눌러야** 탐지 | **워처가 상시 감시**<br>파일 떨어지면 자동 처리 | 사람이 화면을 보고 있어야 탐지되는 구조로는 야간·주말 공백을 못 메움 |
| **판정 코드 위치** | `dashboard.py` 안<br>(`st.session_state` 의존) | `pipeline/detect_service.py`<br>헤드리스 코어 | Streamlit 없이는 재사용 불가했다 → 워처가 복붙하면 판정이 갈라진다 |
| **임계값 근거** | 감(0.60/0.80) | **기대비용 최적(0.005/0.90)**<br>제약 Macro-F1≥0.6 | 성능 지표만으로는 못 정한다. 미탐 1건 1,700만원 vs 오탐 1건 5만원 — 340배 차이를 반영해야 한다 |
| **장애 인지** | 아무도 모름 | **데드맨 스위치**<br>스케줄러 10분 점검 | 패널에 🔴가 떠도 **누군가 봐야** 안다. 새벽에 죽으면 아침까지 공백 |
| **에이전트 시야** | 화면 스냅샷만 | **DB·설정 사실 주입**<br>+ 자가진단 7단계 | 화면에 없는 건 "표시되지 않았다"밖에 못 답했다. 워처는 화면 밖에서 돌므로 아예 답이 불가능 |
| **에이전트 권한** | 화면 조작 17종 | **21종**(워처 제어 4종 추가)<br>중지·재처리는 승인 카드 | 자연어는 "꺼줘"와 "끄면 어떻게 돼?"를 헷갈린다. 되돌릴 수 없는 결과에는 사람 확인을 끼운다 |
| **판정 피드백** | 없음<br>(알림이 맞았는지 아무도 기록 안 함) | **관제 콘솔 판정 루프**<br>정탐/오탐/미탐/보류 + 사유 코드 7종 | 기록이 없으면 임계값을 조정할 **근거가 영원히 안 생긴다**. `batch_analyzer.py:491`의 TODO가 그것이었다 |
| **임계값 산출** | 검증셋(정적 라벨) 기준 | **실제 판정 기반 비용 계산** | 검증셋과 운영 분포는 다르다. 담당자가 찍은 값이 진짜다 |
| **우선순위** | 위험점수 순 | **대기시간 순 + SLA 표시** | 점수 높은 건은 이미 누가 봤을 확률이 높고, **방치된 건은 아무도 안 봤다** |
| **다중 사용자** | 고려 없음<br>(둘이 판정하면 두 줄이 쌓임) | **협조적 잠금**(TTL 15분)<br>+ 사람별 임시저장 | 어느 결론이 맞는지 나중에 아무도 모른다. 강제 잠금은 워처 쓰기까지 물려 위험 |
| **상태 영속** | `session_state`만<br>(새로고침이면 소멸) | **DB 임시저장** | "켜 두는" 도구에서 새로고침 한 번에 메모가 날아가면 치명적 |
| **교대 근무** | (개념 없음) | **인수인계 + 요약 스냅샷** | 야간 근무자가 무엇을 넘겼는지 다음 사람이 알아야 한다 |
| **탐지 결과 UI** | 세션5에만 존재<br>(관제 화면은 `st.metric` 3개) | **`detect_ui.py` 공용 모듈**<br>두 앱이 같은 함수 호출 | 복붙하면 한쪽만 고치는 사고가 반드시 난다 |

---

## 발표 시 강조 포인트 3가지

**1. "판정"과 "설명"을 분리했다**
규칙만으로 사기를 잡으려 하면 정밀도가 0.3%로 무너진다 — 정상 거래도 같은 특징을 갖기 때문(수취계좌 정지 49% · 미사용계좌 51%). 그래서 **판정은 ML, 설명은 규칙**으로 역할을 나눴다. 규칙은 유형 판별에서 무작위 대비 3.2배 성능을 낸다.

**2. 환각을 구조로 막았다**
프롬프트에 "지어내지 마라"라고 쓰는 대신, **인용할 실제 값을 프롬프트에 넣었다**. 규칙 대조 결과(`Distance=338km`)와 RAG 근거 문서(122청크)를 함께 제공하고, "✅충족 항목만 인용하라"고 지시한다.

**3. 어느 단계가 죽어도 파이프라인은 돈다**
모델 없음 → 더미 모드 · LLM 없음 → 템플릿 폴백 · RAG 없음 → 기본 문서 · SMTP 465 차단 → 587 재시도 · 마이크 불가 → 파일 업로드. 시연 중 무엇이 끊겨도 **화면은 계속 돌아간다.**

---

## ④ 알림 전송 기술 — 상세 (질의응답 대비)

### ⚠️ MCP는 현재 사용하지 않습니다

옛 모식도의 `MCP SMTP/Mail Server`는 **현재 코드베이스에 존재하지 않습니다.**
(전 코드 `grep -rn "mcp\|MCP"` 결과 0건 — `notifier.py`는 표준 라이브러리 직결)

| | 옛 설계 | 현재 구현 |
|---|---|---|
| 메일 | LLM → MCP 프로토콜 → MCP SMTP 서버 → 메일 | LLM → 파이썬 `smtplib` → SMTP 서버 → 메일 |
| Slack | (없음) | `requests.post` → Slack Incoming Webhook |

**직결로 바꾼 이유 3가지**
1. **장애점 감소** — MCP 서버가 죽으면 알림 전체가 멈춤. 직결이면 실패 지점이 SMTP 하나
2. **사내 방화벽 대응** — 465 포트가 막힌 환경에서 **587 STARTTLS 자동 재시도**가 필요했는데, 중간 서버를 거치면 이 폴백을 제어할 수 없음
3. **LLM 비의존** — MCP는 LLM이 Tool을 호출하는 구조라 **LLM이 죽으면 알림도 못 감**. 현재는 LLM 실패 시에도 템플릿 폴백으로 보고서를 만들어 **알림은 반드시 나감**

> 발표에서 질문받으면: *"MCP는 LLM이 도구를 호출하는 규격입니다. 저희는 알림이 LLM 생존 여부와 무관하게 나가야 한다고 판단해서, LLM은 **본문 생성만** 담당하고 **발송은 파이프라인이 직접** 하도록 분리했습니다."*

### 실제 사용 기술 정리

```
📧 이메일
   smtplib.SMTP_SSL(host, 465)          암시적 TLS
   smtplib.SMTP(host, 587) + starttls() 명시적 TLS  ← 465 실패 시 자동 폴백
   email.message.EmailMessage
     └ multipart/alternative
         ├ text/plain   HTML 미지원 클라이언트용 폴백 본문
         ├ text/html    시각화 포함 본문
         └ attachment   HTML 리포트 · 차트 (첨부 1건 실패가 발송 전체를 막지 않음)
   실패 사유 분류기: 앱 비밀번호 · 수신거부 · SSL 포트 불일치 · 방화벽 타임아웃

💬 Slack
   requests.post(webhook_url, json={"text": …}, timeout=10)
   Incoming Webhook (HTTPS) · mrkdwn 서식
   Block Kit 미사용 — 텍스트 + 유니코드 막대(████░░)로 시각화해 의존성 0
   웹훅 URL은 화면·로그에 .../services/*** 로 마스킹

🧠 LLM  (발송이 아닌 '본문 생성'만 담당)
   HTTP POST /v1/chat/completions  (OpenAI 호환 규격)
     · llama.cpp 로컬  http://localhost:8080
     · Claude / GPT / DeepSeek / Moonshot / 커스텀 게이트웨이
   교체는 사이드바 드롭다운 한 번 — 코드 수정 불필요

📊 시각화
   외부 이미지 서버·렌더링 엔진 없음. 순수 텍스트·HTML로 생성
   → 이미지 차단 정책이 있는 사내 메일에서도 그대로 표시됨
```

### 이중 임계값 — 등급별 동작표

| 구간 | 등급 | Slack | Email | 메시지 문체 | 이메일 제목 |
|---|---|:---:|:---:|---|---|
| `risk < T₁` | 🟢 `none` | — | — | 자동 발송 생략 | — |
| `T₁ ≤ risk < T₂` | 🟡 `review` | ✅ | — | 의심 · **담당자 검토 요청** | `[검토요청] X형 위험 0.xx` |
| `risk ≥ T₂` | 🔴 `confirm` | ✅ | ✅ | 확정 · **즉시 대응 지시** | `[확정] X형 위험 0.xx` |
| (모드 OFF) | ⚪ `single` | ✅ | ✅ | 기존 단일 임계값 동작 | `[FDS 경보] X형` |

기본값 `T₁ = 0.60` · `T₂ = 0.80` (사이드바에서 조정, AI 챗봇으로도 변경 가능)
`T₂ < T₁`로 잘못 설정하면 `T₂ = max(T₁, T₂)`로 자동 보정하고 경고를 표시합니다.

---

## ⑤ 에이전트 승인 게이트 — 상세

### 액션을 "되돌릴 수 있는가"로 나눈다

```
┌─ 가역 액션 15종 ──────────────────────────────────────── 즉시 실행 ─┐
│  화면 이동   goto_session · goto_s5_tab · goto_batch_subtab          │
│  입력 조작   set_manual_field · set_manual_flag · autofill_high_risk │
│  실행       run_detection · run_batch                               │
│  설정       set_threshold · select_model · select_dataset           │
│             set_eval_mode · set_pii_level · set_compact_mode        │
│             set_beginner_mode                                       │
│  → 잘못 실행돼도 되돌리면 그만. 확인을 요구하면 오히려 방해가 된다        │
└─────────────────────────────────────────────────────────────────────┘

┌─ 비가역 액션 2종 ────────────────────────── 🚦 승인 게이트 통과 필요 ─┐
│  request_send(slack | email | both)                                 │
│  cancel_send()                                                      │
│  → 메일은 한 번 나가면 회수 불가. LLM 오판의 비용이 압도적으로 크다      │
└─────────────────────────────────────────────────────────────────────┘
```

### 게이트 ON (기본값)

```
사용자: "이 거래 담당자한테 메일 보내줘"
   │
   ▼  LLM 응답에 [[ACTION: request_send(email)]] 포함
   │
   ▼  대시보드: _send_request 적재 + 세션5로 이동   ← 발송 코드 미실행
   │
   ▼
 ┌────────────────────────────────────────────┐
 │ 📨 챗봇이 발송을 요청했습니다 — 승인이 필요합니다 │
 │   채널   : EMAIL                            │
 │   수신   : fds-team@company.com             │
 │   제목   : [확정] E형 위험 0.9312            │
 │   마스킹 : standard                          │
 │   📄 발송 본문 미리보기 ▼                     │
 │   [ ✅ 승인하고 발송 ]   [ 취소 ]             │
 └────────────────────────────────────────────┘
   │
   ▼  사람이 승인 → SMTP / Slack Webhook 전송 → 채널별 성공·실패 개별 표시
```

### 게이트 OFF (⚡ 즉시 발송 모드)

빠른 업무를 선호하는 사용자를 위해 **세션5 → 자동 발송 설정**에서 끌 수 있다.
끄더라도 다음 세 가지는 유지된다:

| 유지되는 것 | 내용 |
|---|---|
| **PII 마스킹** | 컴포저 내부에서 그대로 적용 — 게이트와 무관 |
| **감사 로그** | `🧾 게이트 없이 발송된 기록` — 시각·채널·유형·위험도·수신자·마스킹 수준 |
| **경고 표시** | 토글 아래 상시 경고. 마스킹까지 OFF면 🔴 위험 조합 경고 추가 |

```
  ✅ 기본값(키 없음)   → 게이트 적재 O · 즉시 발송 X
  ✅ 게이트 ON 명시    → 게이트 적재 O · 즉시 발송 X
  ✅ 게이트 OFF        → 게이트 적재 X · 즉시 발송 O
  ✅ 탐지 결과 없이 요청 → 두 모드 모두 차단 ("먼저 탐지를 실행하세요")
```

### 발표 포인트

> *"에이전트에게 자율성을 주되, **되돌릴 수 없는 작업에만** 사람 승인을 요구했습니다.
> 모든 액션에 확인을 붙이면 에이전트를 쓸 이유가 없어지고, 아무 것에도 안 붙이면
> LLM 오판이 실제 피해가 됩니다. 그래서 **가역성**을 기준으로 나눴습니다.
> 게이트는 끌 수 있지만, 껐을 때도 마스킹과 감사 로그는 유지됩니다."*

---

## ⑥ 관제 콘솔 판정 루프 — 상세 (v20)

### 왜 이게 "파이프라인의 마지막 조각"인가

v19까지의 파이프라인은 **단방향**이었다.

```
데이터 → 전처리 → 추론 → 임계값 → 알림 발송 → (끝)
```

알림이 나간 뒤 **그것이 맞았는지 틀렸는지 아무도 기록하지 않았다.**
그래서 임계값을 조정하려면 언제나 **검증셋(과거의 정적 라벨)** 로 돌아가야 했다.
운영 분포가 검증셋과 다르면(거의 항상 다르다) 그 값은 추정치일 뿐이다.

v20의 관제 콘솔이 이 고리를 닫는다.

```
데이터 → 전처리 → 추론 → 임계값 → 알림 발송 ─┐
  ▲                                          │
  │                                          ▼
  └─── 임계값 재계산 ◄── 오탐 분석 ◄── 사람의 판정
       (실판정 비용)      (사유 집계)     (정탐/오탐)
```

### 판정 1건이 지나가는 길

```
┌────────────────────────────────────────────────────────────────────────┐
│ 워처가 알림 생성                                                         │
│   transactions(원장) + detections(결과) + analysis_cache(LLM 근거)       │
└────────────────────────────┬───────────────────────────────────────────┘
                             ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 🚨 트리아지 큐  ops_queries.alert_queue()                                │
│   조건: is_anomaly=1 OR risk_score >= min_score                         │
│         (점수가 낮아도 예측 유형이 사기면 review 승격 — 미탐 안전망)        │
│   ⏱ ops_shift.annotate()  → age_min · elapsed · urgency(ok/warn/over)   │
│   정렬 기본값: 대기순 (점수순 아님)                                        │
└────────────────────────────┬───────────────────────────────────────────┘
                             ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 담당자가 알림을 펼침                                                      │
│   🔒 review_store.claim()   → alert_claim (txn_id, reviewer, 지금)       │
│      · 남이 TTL(15분) 안에 잡고 있으면 경고 + 차단                        │
│      · TTL 지났으면 뺏어옴 (브라우저를 닫고 간 경우)                       │
│   💾 review_store.load_drafts() → 이전에 쓰다 만 입력 복원                 │
└────────────────────────────┬───────────────────────────────────────────┘
                             ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 판정/사유/메모 입력 (on_change 마다)                                      │
│   🔒 claim 갱신(하트비트)  +  💾 save_draft()                            │
│      → review_draft (txn_id + reviewer 복합 PK · 사람마다 따로)           │
│   ※ 바뀐 행 하나만 쓴다 — 매 rerun 20행 쓰기 낭비 없음                     │
└────────────────────────────┬───────────────────────────────────────────┘
                             ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 저장  review_store.record()                                             │
│   INSERT alert_review (판정 + **당시 스냅샷**: tier·score·th₁·th₂·model)  │
│   DELETE alert_claim   (잠금 해제)   ┐ 같은 트랜잭션                      │
│   DELETE review_draft  (임시저장 정리)┘ — "기록됐는데 잠금이 남는" 상태 방지 │
│   ⚠️ 같은 txn_id 재판정은 **덮어쓰지 않고 새 행**. 번복 자체가 감사 대상    │
└────────────────────────────┬───────────────────────────────────────────┘
                             ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 📉 오탐 분석 — 사유 코드별 집계                                           │
│   오탐률 = fp / (tp + fp)   ※ 보류·미탐은 분모에서 제외                   │
│   (보류를 정탐 취급하면 오탐률이 실제보다 낮게 보인다 — 가장 흔한 왜곡)      │
└────────────────────────────┬───────────────────────────────────────────┘
                             ▼
┌────────────────────────────────────────────────────────────────────────┐
│ ⚙ 임계값 튜닝 — 실판정 기반 기대비용                                      │
│   비용 = 오탐수 × 오탐단가 + 미탐수 × 미탐단가                            │
│   최소 지점을 추천 → watcher_config 저장 → 워처가 5초 내 반영             │
└────────────────────────────────────────────────────────────────────────┘
```

### 시간 축 — SLA와 교대

```
  09:00 ─────────────────────────────────────────────────── 17:00
   │                                                          │
   ├─ 🔄 근무 시작                                             ├─ 🔄 근무 종료
   │   앞사람 인수인계 읽기                                      │   요약 확인
   │   (shift_handover 최신 1건 + 당시 스냅샷)                   │   메모 작성 → 저장
   │                                                          │
   └──────────────── 🚨 트리아지 루프 ────────────────────────┘
                     ⏱ SLA 30분 기준
                     🟢 정상   🟡 24분↑ 임박   🔴 30분↑ 초과
                     KPI: 대기 / 초과 / 임박 / 최장대기
```

### 발송 등급 vs 경보 등급 — 이름이 비슷한 두 축

발표 중 가장 많이 나오는 혼동이다. **방향이 반대다.**

| | `ops_dispatch` (나가는 통보) | `ops_alert` (들어오는 경보) |
|---|---|---|
| 목적 | 화면 앞에 **없어도** 도달 | 자리에 **있을 때** 눈치채게 |
| 수단 | Slack · Email 푸시 | 소리 · 데스크톱 알림 · 화면 팝업 |
| 등급 | `none` / `review` / `single` / `confirm` | `confirm` / `review` / `all` |
| 트리거 | 탐지 직후 (자동 발송 토글) | DB 폴링 (5초) |
| 억제 | 등급 미달 시 생략 | 조용한 시간 · 중복 억제 · 최대 버스트 |

```
       탐지
        │
        ├──► ops_dispatch ──► Slack/Email ──► 📤 발송 감사 로그
        │    (나간다)                          누가·언제·어디로·마스킹
        │
        └──► DB 적재 ──► ops_alert 폴링 ──► 🔔 소리/알림/팝업
             (들어온다)                      조용한시간·중복억제 적용
```

### 발표 포인트

1. **"모델을 고치지 않고도 성능이 오릅니다"**
   임계값은 모델 재학습 없이 즉시 바꿀 수 있는 유일한 손잡이다. 그런데 그 손잡이를
   **근거 있게** 돌리려면 실제 판정 데이터가 필요하다. 이 루프가 그것을 만든다.

2. **"오탐률보다 대기시간이 먼저입니다"**
   기본 정렬을 점수순이 아니라 대기순으로 둔 이유. 점수 높은 건은 이미 누가 봤을
   확률이 높고, 방치된 건은 아무도 안 봤다.

3. **"판정은 지우지 않습니다"**
   `record()`는 같은 거래를 다시 판정해도 덮어쓰지 않고 새 행을 쌓는다.
   번복 자체가 감사 대상이기 때문이다.

4. **"둘이 같은 알림을 판정하면 어느 쪽이 맞는지 아무도 모릅니다"**
   그래서 협조적 잠금을 뒀다. 강제로 막지 않는 이유는 DB 레벨 잠금이
   워처의 쓰기까지 물릴 수 있어서다 — 관제를 지키려다 탐지를 멈추면 본말전도다.

### 공용 모듈 — 두 대시보드가 같은 화면을 쓴다

```
                 ┌──────────────────────┐
                 │  pipeline/detect_ui  │  게이지·확률막대·유형카드
                 │  (테마 T를 인자로)     │  규칙 패널·판정 배너·TTS·이력
                 └──────┬────────┬──────┘
                        │        │
          dashboard.py ─┘        └─ ops_dashboard.py
          :8501 THEMES              :8502 ops_ui.THEMES
          (검증셋 데이터)             (라이브 DB 데이터)
```

같은 함수, 다른 테마, 다른 데이터 소스. 게이지 모양을 고치면 두 앱에 함께 반영된다.
`asset_registry`(모델·데이터셋 탐색)도 같은 방식으로 공유한다.


---

## ⑦ 경보 파이프라인 (v22)

```
   워처 탐지 ─┐                    ┌─ AI 분석 탭 수동 탐지
              ▼                    ▼
        ┌──────────────────────────────┐
        │  ops_alert.poll_new / fire   │  등급·중복·조용한시간·버스트 필터
        └───────┬──────────────┬───────┘
                │              │
     ┌──────────▼─────────┐   ┌──▼──────────────────┐
     │ 🛰 레이더 카드(우상단)│   │  · 삐- 소리 (1~10회) │
     │  ✕  = 인라인 속성    │   │  · 데스크톱 알림      │
     │  소멸 = CSS 애니메이션│   └──────────┬──────────┘
     │  이동 = <a href>     │              │
     └──────────┬──────────┘              │
                └───────────┬─────────────┘
                            ▼
                    🗃 탐지 로그로 이동
                    (그 건이 무엇이었나)

     [폴백] 📋 상단 배너 (기본 OFF) — st.button · 트리아지로 이동
```

**왜 클로저를 안 쓰나** — Streamlit 은 리런마다 컴포넌트 iframe 을 파괴한다.
그 안에서 만든 `onclick` 클로저와 `setTimeout` 은 함께 죽는데, 카드 DOM 은
부모 문서에 남아 **화면에는 그대로 보인다** — "안 사라지고 버튼도 안 눌리는" 상태.
인라인 `onclick` **속성**은 부모 문서 realm 에서 컴파일되므로 이 함정을 피한다.

**왜 탐지 로그로 가나** — 경보를 누르는 사람이 알고 싶은 것은
"그 건이 무엇이었나"(당시 데이터·분석)다. 판정은 그다음이다.
