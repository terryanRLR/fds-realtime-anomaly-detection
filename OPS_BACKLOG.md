# FDS 관제 콘솔 — 미해결 과제 목록

`PATCH_NOTES5.md` 는 **끝난 일의 기록**이라 남은 일이 버전 섹션마다 흩어진다.
이 파일은 **아직 안 한 일만** 모아 둔다. 끝나면 여기서 지우고 패치 노트로 옮긴다.

## 현재 열린 과제 — 2건 (보류 1 · 조사 1)

### B2. 비한국어 화면의 **연속 렌더가 자동 검증되지 않는다** (v39에서 발견)

`AppTest` 로 화면 언어를 한국어 외로 바꾼 뒤 **두 번째** `run()` 을 하면
하네스가 죽는다.

```
ValueError: '🚨 알림 큐 (미판정)' is not in list
  streamlit/testing/v1/element_tree.py  Radio.index
  → self.options.index(self.format_func(self.value))
```

대상은 AI 탭의 `batch_src` 라디오다. 옵션은 `["queue","handoff"]`(안정적인 키)
이고 라벨만 `format_func` 으로 번역하는데, AppTest 가 **포맷된 라벨 목록**에
`format_func(value)` 를 찾으면서 어긋난다.

**v39 작업과 무관한 기존 현상이다.** 확인한 방법 —

| 재현 경로 | 결과 |
|---|---|
| 단축키 블록을 통째로 들어낸 사본 | **동일하게 재현** |
| 기존 사이드바 언어 라디오(`_lang_pick`)만 조작 | **동일하게 재현** |
| `lang="en"` 으로 처음부터 띄우고 그냥 두 번 run | **동일하게 재현** |

→ 실제 브라우저에서는 세션 값 `"queue"` 가 옵션 안에 있으므로 **영향 없다.**
막히는 것은 테스트다: `selftest_ui.py` 가 비한국어 상태를 한 번밖에 못 그려서,
**언어 전환 이후의 화면 회귀를 자동으로 잡지 못한다**(§11.3 i18n 작업의 사각지대).

손댈 때 선택지는 둘이다.
1. `batch_src` 를 `format_func` 대신 **번역된 옵션 리스트 + 인덱스**로 바꾼다
   — 앱 코드를 하네스에 맞추는 셈이라 다른 `format_func` 위젯과 일관성이 깨진다
2. 테스트에서 언어별로 **매번 새 AppTest 를 만든다**(연속 run 을 피한다)
   — 하네스 우회. 지금 `selftest_ui.py` 의 `⌨` 절이 임시로 쓰는 방식이다

> 급하지 않다. 지금 막히는 실사용이 없고, 원인·재현·영향 범위가 위에 확정돼 있다.
> 다만 **i18n 회귀를 자동으로 못 잡는다**는 사실은 남아 있으므로 지우지 말 것.

### B1. 피처 사전 다국어화 — **보류 (2026-08-10, 사용자 판단)**

계좌 이력 피처 이름(`최근 1개월 최대 거래액` 등)은 영어 화면에 남은 마지막
한국어 5개다. **화면만 번역하면 안 된다** — 같은 이름을 넷이 공유한다.

| 같이 움직여야 하는 것 | 왜 |
|---|---|
| `pipeline/detect_io.py` `ACCOUNT_HISTORY_FIELDS` | 화면 라벨의 출처 |
| `model_meta.json` | 모델 피처 사전. 이름이 갈리면 모델과 화면이 다른 말을 한다 |
| `tools/threshold_report.py` 산출물 | 리포트와 화면을 **대조할 수 없게 된다** |
| `ops_dashboard_사용설명서.md` | 실무자가 화면·리포트·문서를 오가며 읽는다 |

→ 화면 i18n 작업이 아니라 **사전 마이그레이션**이다. 손댈 때 넷을 한 번에.
`detect_io.py`의 `ACCOUNT_HISTORY_FIELDS` 위에 이 이유가 주석으로 박혀 있다.

---

## 일부러 안 하기로 한 것 (과제 아님)

| 항목 | 왜 안 하나 |
|---|---|
| 세션5 나머지 입력 모드(test/train/synthetic/folder) | 아직 두 벌이지만 **알려진 버그가 없고**, dashboard 고유 기능(`t3_src` 원본CSV/Parquet 선택 · 컴팩트 뷰 · `_seg_nav`)이 있어 합치면 오히려 잃는다. 그쪽을 손댈 일이 생기면 그때 |
| 발송 경로 | **의도적으로 두 벌.** ops=편집 가능한 평문+감사로그 / dashboard=리치 HTML+마스킹 첨부. 합치려면 "이메일이 어떤 물건인가"부터 정해야 하는 제품 결정이다 |
| 인수인계서 **문서 본문** 번역 | 저장·배포되는 산출물이라 화면 언어를 따라가면 안 된다. **화면 UI만** 번역했다 |

> 코드를 손대기 전후로 **`python -m pipeline.selftest_all`**
> (전체 12종 80초 · `--fast` 는 UI·원본대조를 빼고 6초)
>
> 새 과제가 생기면 이 파일 맨 위에 적고, 끝나면 '최근에 닫힌 것' 으로 옮긴다.

*최종 갱신: 2026-08-10 (v39)*

---

## ✅ v28~v37 에서 닫힌 것 — 실무자 사용성 점검 반영

점검 9건 + i18n 전면(8단계) + 미탐 곡선 · 압축 탭. 상세는 `PATCH_NOTES5.md` §v28~v37.

| 항목 | 결과 |
|---|---|
| 임계값 슬라이더가 `0.005`를 못 만듦 (**실버그**) | `정확한 값` 숫자 칸이 실제 적용값 |
| 되돌릴 수 없는 동작에 확인 없음 | 임계값 저장 + 발송 6곳 = **7곳 확인 카드**, 구현은 한 벌 |
| 미탐 기록 창구 부재 (집계가 늘 0) | 📉 오탐 분석에서 거래 ID 직접 등록 |
| 화면 절반만 번역됨 | 571키 × 4개국어 · 영어 화면 한국어 94 → **5** |
| 기대비용 곡선이 미탐을 못 봄 | `threshold_whatif_fn()` **별도 함수**로 겹쳐 그림 (기존 곡선 불변) |
| 탭 8개가 노트북에서 넘침 | 압축 라벨 (한국어 1,096 → 728px) · 기본 꺼짐 |
| 경보 스위치를 못 찾음 | 사이드바로 이동 (세부 설정은 그대로) |
| 편집기가 작업대 첫 화면을 차지 | 사이드바로 이동 |

> **확정 요구사항 불변** — 첫 탭 `🧠 AI 분석` · `th_review = 0.005`.
> 비교용 배치 전환은 세션 한정이고 파일에 아무것도 쓰지 않는다.

---

## ✅ v27(A2) 에서 닫힌 것 — 편집기 계층 공용화

| 항목 | 결과 |
|---|---|
| 프롬프트 편집기 두 벌 | `dwb.render_prompt_editor()` 한 벌 |
| RAG 편집기 두 벌 | `dwb.render_rag_editor()` 한 벌 |
| `prompt_overrides` 4키 dict 두 벌 | `dwb.prompt_overrides()` 한 벌 |
| ops 의 새 문서 이름 검증 부재 | dashboard 구현이 더 완전해서 **ops 가 검증 3종을 얻었다**(빈 이름·경로 탈출·중복) |
| 재발 방지 | `selftest_ui` §14 가 **정적 검사**로 막는다 — 앱 파일에 `_PROMPT_SLOTS`/`rag_reidx_` 가 다시 나타나면 실패 |

**결과: ops −117줄 · dashboard −133줄 · 공용 모듈 +314줄 = 합계 +64줄.**
줄 수를 줄이는 게 목적이 아니었다 — 구현이 두 벌에서 한 벌이 된 것이 결과다.

---

## 📋 §A2 조사 기록 — 왜 범위를 바꿨나 (v26에서 수행)

### 결론: 원래 정의(단건/배치/챗 본문 공용화)는 **하면 안 된다**

발송 경로를 실측해 보니 두 앱은 **설계상 다르다** — 우연히 갈린 게 아니다.

| | ops_dashboard | dashboard.py |
|---|---|---|
| 이메일 본문 | 평문 + **편집 가능한 미리보기**(보이는 그대로 발송) | 리치 HTML + 등급 머리말 |
| 첨부 | 없음 | **강제 마스킹된 HTML 리포트** |
| 발송 기록 | `ops_dispatch` 감사 로그 | 없음 |
| 목적 | 관제 담당자가 손봐서 보낸다 | 보고용 산출물을 만든다 |

합치려면 "이메일을 어떤 물건으로 볼 것인가"를 하나로 정해야 한다. 이건 리팩터링이
아니라 **제품 결정**이므로 코드로 밀어붙이면 안 된다. → **A2 범위에서 제외.**

### 진짜 공용화 대상 — 편집기 계층 (두 앱이 거의 동일)

| 대상 | 상태 | 비고 |
|---|---|---|
| 🖊 프롬프트 편집기 | 두 벌 (~55줄 × 2) | 슬롯 4종·저장/복원 로직 동일. i18n 키만 `s5.*` vs `ai.*` |
| 📚 RAG 편집기 | 두 벌 (~75줄 × 2) | 파일 CRUD·재색인 동일 |
| `prompt_overrides` dict | 두 벌 | `_build_llm_analyzer` 안에 각각 하드코딩된 4키 dict |

`sync_widget` 처럼 **`detect_workbench` 에 `render_prompt_editor()` /
`render_rag_editor()` / `prompt_overrides()` 를 두고 양쪽이 부르면 된다.**
발송 경로를 안 건드리므로 A1 과 같은 난이도다.
→ **v27 에서 그대로 실행했다.** (위 'v27(A2) 에서 닫힌 것' 참조)

### ⚠️ 이 조사에서 나온 교훈 — 같은 병이 3번 나왔다

`value=` 함정(Streamlit 은 key 가 있으면 `value=` 를 무시한다)이 **같은 편집기
코드가 두 벌**이라는 이유로 세 번 반복됐다.

| # | 위치 | 발견 | 증상 |
|---|---|---|---|
| 1 | ops 이메일 미리보기 | v24 | 보이는 본문과 **실제 발송 본문이 달랐다** |
| 2 | dashboard 프롬프트 편집기 | v26 | '기본값 복원'이 화면에 반영 안 됨 |
| 3 | dashboard RAG 편집기 | v26 | 파일이 바뀌어도 편집창은 옛 내용 → **저장 시 남의 수정을 되돌림** |

**셋 다 예외가 안 난다.** 그래서 눈으로는 못 찾고, 코드가 두 벌인 한 계속 재발한다.
A2 를 하는 이유는 '줄 수 줄이기'가 아니라 이것이다.

> 코드를 손대기 전후로 **`python -m pipeline.selftest_all`**
> (전체 12종 78초 · `--fast` 는 UI·원본대조를 빼고 6초)
>
> 새 과제가 생기면 이 파일 맨 위에 적고, 끝나면 '최근에 닫힌 것' 으로 옮긴다.

*최종 갱신: 2026-08-09 (v26)*

---

## ✅ v26(A1) 에서 닫힌 것 — 직접입력 계약 통일

| 항목 | 결과 |
|---|---|
| dashboard.py 자동입력 버튼이 **눌리면 죽었다** | 수정 — 위젯 생성 후 key 수정 → `StreamlitAPIException`. 예외로 그 아래 `[탐지 실행]` 렌더까지 끊겨 **버튼이 화면에서 사라졌다.** 예약값 패턴으로 전환 |
| dashboard.py row 에 **계좌 이력이 없었다** | 수정 — 번들 기본값 0 이 쓰여 '거래가 전혀 없던 계좌'가 되고, 고위험 프리셋조차 정상(m)으로 판정됐다. `build_manual_row()` 가 항상 넣는다 |
| 자동채움 값 세트가 두 벌 | `AUTOFILL_PRESET` + `AUTOFILL_FLAGS` 로 통일(합집합 5종). ops 점수 0.8781→0.8773 미세 변동 |
| 챗 액션 `_FIELD_WK` 가 렌더와 따로 | `_S5_FIELD_WK` 한 벌 참조로 통일 |
| dashboard.py RAG 편집기 `value=` 버그 | 수정 (§A2 준비 중 발견) |

> **검증:** 두 앱이 같은 입력에 **같은 판정** — `d / 0.8773517059743291`.
> A1 이 필요했다는 가장 직접적인 증거다. `selftest_ui` §13 으로 고정.

---

## ✅ v26 에서 닫힌 것 — 조립 계층 공용화

| 항목 | 결과 |
|---|---|
| AI 탭 분리 (구 과제 A) | **`pipeline/detect_workbench.py` 신설.** '탐지 입력' 624줄 → 호출부 33줄. ops_dashboard 3,061→2,730줄 |
| 파일만 옮기는 안(B) 검토 | **기각.** 헬퍼 7종이 전부 탭 안팎 공유라 인자 7개짜리 함수가 될 뿐, 이중화는 그대로 |
| dashboard.py 프롬프트 편집기 `value=` 버그 | 수정 — v24 에 ops 만 고쳐지고 이쪽엔 남아 있던 것 |
| dashboard.py 테스트 커버리지 0 | `selftest_ui` §11 스모크 신설 (기동·세션5·프롬프트 복원 회귀) |
| 이관 동일성 보장 | `selftest_ui` §12 — 위젯 key 27종·입력 탭 6종·챗봇 액션 2종 |

---

## ✅ v25 에서 닫힌 것 — 화면 동선

| 항목 | 결과 |
|---|---|
| 사이드바 배치가 빈도와 반대 | **3층 재배치.** 검토자가 8번째 → 1번째. 최초 1회 설정은 `⚙ 고급 설정` 한 덩어리로 |
| 밀린 일이 트리아지 탭에만 보임 | **헤더 상시 배지** — 미판정·SLA 초과·최장 대기·판정자 |
| 임계값 3종이 각자 화면에만 있음 | **`ui.threshold_matrix()` 대조표** — 사이드바 + 튜닝 탭 |
| AI 탭이 편집기부터 보여줌 | 작업 서브탭을 위로 (`st.tabs()` 생성 위치만 이동) |
| 챗봇 `set_sla` 가 조용히 실패 | `_pending_sla` 예약값으로 전환 + `selftest_agent` §6 확장 |
| 워처 배지가 다른 DB 를 읽음 | `render_watcher_badge(db_path, poll_interval)` 인자 전달 |
| 모델 폴더 변경이 한 박자 늦음 | 관제 설정(`model_dir`)이 모델 섹션 위로 가면서 해소 |

---

## 🔴 오빠 결정 대기

**없음** — 두 건 모두 결론이 났다.

| 결정 | 결과 |
|---|---|
| D1 원장 백필 (과거 탐지 72건) | **되살리지 않기로 함.** 도구(`tools/backfill_ops_ledger.py`)는 남겨 뒀으니 나중에 필요하면 `--days N --apply` |
| D2 감사 로그 영속화 | **완료 (v24).** `notify_audit` 테이블 + 2단계 확인 삭제 + 삭제 흔적 기록 |

---

## 🟠 점검에서 나왔으나 미착수

**없음** — v24 점검 10건, v25 동선 7건을 모두 닫았다.
남은 것은 위 표의 **A(AI 탭 분리)** 하나뿐이다.

---

## 🟡 문서에 이미 적혀 있던 과제 (v22 이전 이월)

**전부 닫혔다.** (v24)

| 항목 | 결과 |
|---|---|
| 잠금 하트비트 | `renew_claims()` + 트리아지 탭 fragment 하트비트 |
| dashboard.py 온보딩 | 지속 플래그 패턴으로 수정 (추정 → 확인 → 수정) |
| LLM 액션 준수율 | 마커 표기 관용 + 프롬프트 예시 (의도 추론은 하지 않음) |
| selftest 커버리지 | 12종 · `python -m pipeline.selftest_all` |
| `Location_region` 매핑 | 17종 전수 실측 확정 + 명시화 |
| `feature_defaults.json` | 직접입력 계좌 이력 배선(B) + UI 노출(C) 로 우회 — 번들은 불변 |
| 클라우드 워처 상태 | `pipeline/status_push.py` — 파일/HTTP 푸시 + 대시보드 폴백 |

### 남은 것

**없음.** 낮은 우선순위 4건도 v24 에서 정리했다 (PATCH_NOTES5 §14).

| 항목 | 결론 |
|---|---|
| `Time_difference_seconds` 99.866% | **수정** — 음수 경과시간을 NaN 으로. 58피처 전부 100% |
| `FeatureBridge` 축소 | **유지** — dashboard 세션2 에서 실제로 쓰인다(제거 조건 불성립) |
| `MODEL_REGISTRY` 정리 | **불필요** — 파일 없으면 이미 자동 숨김, `type` 미사용 |
| `moonshot` 키 미배선 | **수정** — dashboard 사이드바에 입력란 추가 |

---

## ✅ 최근에 닫힌 것 (참고)

| # | 항목 | 닫힌 시점 |
|---|---|---|
| 1 | 원장 이원화 — 수동 탐지가 관제 화면에 안 보이던 문제 | v24 |
| 2 | 경보 등급이 실제 임계값을 안 읽던 문제 | v24 |
| 3 | 발송 감사 로그 4개 경로 누락 | v24 |
| 4 | 위젯 스테일 — 보이는 것과 보내는 것이 달랐던 문제 | v24 |
| 6 | 경보 카드 딥링크가 그 거래를 열지 않던 문제 | v24 |
| 5 | 검색·필터가 limit 뒤에 걸려 원장 전체를 못 훑던 문제 | v24 |
| 7 | CSV 행 번호가 거래 ID 로 둔갑해 서로 덮어쓰던 문제 | v24 |
| 8 | `watch_inbox` 가 아무도 설정하지 않는 키였던 문제 | v24 |
| 9 | requirements streamlit 하한이 실제보다 낮던 문제 | v24 |
| 10 | 문서의 옛 임계값 표기 | v24 |
| D2 | 감사 로그 영속화 + 2단계 확인 삭제 | v24 |
| B | 직접입력 계좌 이력 배선 — 자동채움이 사기를 만든다 | v24 |
| C | 🏦 계좌 이력 UI 노출 + 되돌리기 | v24 |
| — | 자동채움 버튼이 클릭 시 예외로 죽던 문제 | v24 |
| — | `predict_batch` 단건/배치 불일치(NaN 주입) | v24 |
| — | dashboard.py 온보딩 3버튼 먹통 (추정 → 확인 → 수정) | v24 |
| — | `Location_region` 17개 시도 매핑 확정 + 명시화 | v24 |
| — | 자체 테스트 12종 정착 (`selftest_all`) | v24 |
| — | 잠금 하트비트 · 액션 준수율 · 클라우드 상태 푸시 | v24 |
| — | 미래 하트비트를 '정상'이라 말하던 문제 | v24 |
| — | 낮은 우선순위 4건 (Time_difference 100% 포함) | v24 |
| — | 트리아지 탭 중복키 크래시(표시 100건) | v24 |
| — | selftest 5종 Windows 실행 불가 · 운영 DB 오염 | v24 |
