# 예상 질문 · 답변 모음 — 부서별

> 함께 볼 것: `기술선택_설명자료.md` (MCP·n8n 관련 집중 대응)
> **원칙** — 모르면 "확인해서 알려드리겠습니다". 추측한 숫자를 말하는 순간 나머지도 의심받습니다.

---

## 🔢 0. 외워 갈 숫자 카드

| 항목 | 값 | 출처 |
|---|---|---|
| 모델 | LightGBM 13-class · 58피처 | `model_meta.json` |
| macro F1 | **0.6138** (검증 재현 완료) | `verify_bundle` 4경로 일치 |
| 탐지 재현율 | **89.5%** (사기를 사기로) | `threshold_report` @0.005 |
| 유형 정확도 | **53.5%** (정확한 유형까지) | `verify_bundle` |
| 정밀도 | **79.4%** | 임계값 0.005 기준 |
| 검증셋 | 23,860행 · 사기 258건(**1.08%**) | `X_va.parquet` |
| 오탐 | 23,860행 중 **약 60건** | 임계값 0.005 |
| 하루 예상 알림 | **3.7건** (유입 300건 가정) | `threshold_report --daily 300` |
| 기대비용 | **4.62억** (이전 7.48억) | 미탐×1,700만 + 오탐×5만 |
| 임계값 | 검토 **0.005** / 확정 **0.90** | 기대비용 최적 (제약 F1≥0.6) |
| 감지 주기 | **5초** 폴링 | 워처 |
| 장애 감지 | **10분** 이내 | 데드맨 스위치 |
| 에이전트 액션 | **21종** | `chat_agent.py` |

> ⚠️ **재현율이 두 개인 이유**를 반드시 구분해서 말할 것.
> 82.9% = "사기를 사기로 잡았나(유형 무관)" · 53.5% = "정확한 유형까지 맞췄나".
> 알림 관점은 앞, 모델 성능 관점은 뒤입니다. 섞어 쓰면 지적당합니다.

---

## A. 경영 · 기획

**A1. 이걸로 뭐가 좋아집니까? 한 줄로.**
> 사람이 화면을 보고 있어야만 탐지되던 것을, **파일이 떨어지면 5초 안에 자동으로**
> 판정·분석·통보하게 만들었습니다. 야간·주말 공백이 사라집니다.

**A2. 지금 바로 쓸 수 있습니까?**
> 기능은 완성돼 동작 확인까지 마쳤습니다. 다만 **임계값이 과거 데이터 기준**이라
> 운영 첫 주는 실측치와 대조하며 조정이 필요합니다. 그 기간에도 탐지는 정상 작동합니다.

**A3. 비용이 얼마나 듭니까?**
> 추가 라이선스·클라우드 비용은 **0원**입니다. LLM을 로컬(llama.cpp)로 돌리고
> 알림은 Slack 웹훅·SMTP를 씁니다. 기존 PC 자원만 사용합니다.

**A4. 사람이 하던 일을 없애는 겁니까?**
> 아닙니다. **놓치던 걸 잡는 것**입니다. 지금도 최종 판단과 대응은 담당자가 합니다.
> 시스템은 "이 거래를 보세요"까지만 하고, 근거(규칙 체크리스트·RAG 문서)를 함께 제시합니다.

**A5. 일정이 계획보다 어떻습니까?**
> 계획된 목표(감시→ML→AI분석→알림)는 전부 달성했고,
> 계획에 없던 항목(판정 엔진 단일화·자가 복구·상태 관측)이 추가됐습니다.
> 수단 일부(MCP·n8n)는 검증 후 보류했습니다 — 별도 자료로 설명드리겠습니다.

**A6. 다음 투자는 어디에 필요합니까?**
> **모델 개선**입니다. 임계값 조정으로는 더 못 잡는 미탐이 41건 있고,
> 그중 68%가 4개 유형에 몰려 있습니다. 재학습·피처 보완 영역입니다.

---

## B. 현업 · 운영 담당자

**B1. 하루에 알림이 몇 개나 옵니까?**
> 검증셋 기준 **하루 3.7건**입니다(유입 300건 가정). 그중 헛알림이 1건 미만입니다.
> 다만 **실제 유입의 사기 비율이 다르면 건수도 달라집니다.** 첫 주 실측 후 조정합니다.

**B2. 알림이 너무 많으면요?**
> 대시보드에서 슬라이더로 임계값을 올리면 **재시작 없이 5초 안에** 반영됩니다.
> 챗봇에 "알림 줄여줘"라고 해도 되고, 그때 **몇 건이 줄고 사기 몇 건을 놓치는지**
> 숫자로 알려준 뒤 확인을 받습니다.

**B3. 알림이 오면 저는 뭘 해야 합니까?**
> Slack 메시지에 유형·위험점수·판정 근거·권장 조치가 들어 있습니다.
> 확정(2차) 등급은 이메일도 함께 갑니다. 상세는 대시보드에서 거래 ID로 조회합니다.

**B4. AI가 틀리면요?**
> 틀립니다. 유형 정확도가 53.5%라 **유형은 참고용**으로 보셔야 합니다.
> 다만 "이 거래가 이상하다"는 판정은 82.9% 잡고 정밀도가 97.7%라
> **온 알림은 대체로 실제 이상거래**입니다. 최종 판단은 담당자 몫으로 남겨뒀습니다.

**B5. 같은 거래로 알림이 계속 오지 않습니까?**
> 같은 거래 ID는 **24시간 동안 재알림하지 않습니다.**
> 단, 등급이 검토→확정으로 올라가면 한 번 더 갑니다.

**B6. 밤에 시스템이 죽으면 아침까지 모르는 것 아닙니까?**
> 그래서 **데드맨 스위치**를 뒀습니다. 10분마다 생존을 확인해 끊기면 Slack으로 알립니다.
> 복구되면 복구 알림도 옵니다.

**B7. 잠깐 꺼두고 싶을 때는요?**
> 대시보드에서 끄거나 챗봇에 "30분만 꺼줘"라고 하면 됩니다.
> **끈 사실이 Slack에 통지**되고, 지정한 시간에 자동으로 다시 켜집니다.
> 무기한으로 끄면 6시간 뒤부터 하루 한 번 "아직 꺼져 있습니다" 알림이 옵니다.

**B8. 파일을 잘못 넣었는데 다시 처리하려면?**
> 챗봇에 "OO 파일 재처리해줘"라고 하면 확인 카드가 뜨고, 승인하면 처음부터 다시 읽습니다.

---

## C. 보안 · 컴플라이언스

**C1. 고객 개인정보가 외부로 나갑니까?**
> 나가지 않습니다. LLM을 **로컬(llama.cpp)** 로 돌리고, 로컬이 실패해도
> **외부 API로 자동 전환하지 않습니다**(`cloud_fallback=False`가 기본값).
> 이 조합에서는 아예 전송 경로가 없습니다.

**C2. Slack·이메일로는 뭐가 나갑니까?**
> **마스킹된 값만** 나갑니다. 이름 `이상호→이○○`, IP `171.237.22.26→171.237.*.*`,
> 계좌·식별번호 부분 마스킹. 4단계 레벨이 있고 기본은 `standard`입니다.
> 컬럼명 기반 + 정규식 패턴 기반 **이중 안전장치**입니다.

**C3. DB에는 원본이 저장됩니까?**
> 워처가 적재하는 건 **마스킹본입니다.** 무인으로 대량 적재되므로
> 평문 개인정보가 쌓이는 위험이 더 크다고 판단해 대시보드 경로보다 엄격하게 잡았습니다.

**C4. 음성 입력은 어떻습니까? 마스킹이 안 될 텐데요.**
> 정확한 지적이고, 그래서 **음성은 별도 규칙**을 뒀습니다.
> 음성은 마스킹이 불가능한 원본 개인정보이므로, LLM이 로컬 모드(외부 차단)이면
> **STT도 로컬만 허용**하고 클라우드 STT 요청을 거부합니다.

**C5. 누가 설정을 바꿨는지 추적됩니까?**
> 워처 임계값은 `watcher_config.json`에 **변경 주체·시각**이 기록됩니다.
> 알림 발송은 `notified` 테이블에, 탐지는 `detections`에 전 건 남습니다.

**C6. 대시보드를 외부 공유하면 아무나 조작할 수 있는 것 아닙니까?**
> 그 위험 때문에 **워처 시작·중지 버튼은 기본적으로 잠겨 있습니다.**
> `.env`에 명시적으로 허용 플래그를 넣어야 열립니다.
> 원격 배포(Streamlit Cloud) 환경에서는 자동으로 차단됩니다.

**C7. AI가 마음대로 뭔가를 실행할 수 있습니까?**
> 액션 21종이 **화이트리스트**로 제한돼 있고, 목록에 없는 건 무시됩니다.
> 되돌릴 수 없는 것(발송·워처 중지·파일 재처리)은 **사람이 승인 버튼을 눌러야** 실행됩니다.
> 되돌릴 수 있는 것만 즉시 실행됩니다.

---

## D. IT · 인프라

**D1. 어디서 돌아갑니까? 서버가 필요합니까?**
> 현재는 담당자 PC에서 실행합니다(배치 파일 원클릭).
> 24시간 무인 운영이 필요해지면 Windows 서비스 등록 스크립트가 준비돼 있습니다.

**D2. 감시 폴더가 네트워크 공유여도 됩니까?**
> 됩니다. 오히려 그걸 전제로 설계했습니다. 이벤트 방식(watchdog)은
> **SMB 공유에서 이벤트를 조용히 놓치기 때문에** 5초 폴링을 택했습니다.
> 다만 서비스 등록 시에는 계정 지정이 필요합니다(기본 SYSTEM 계정은 매핑 드라이브 접근 불가).

**D3. 파일이 쓰이는 중에 읽어서 깨지지 않습니까?**
> **수정 후 3초 경과 + 크기 불변**을 확인한 뒤에 읽습니다. 반쪽 파일은 다음 폴링으로 넘깁니다.

**D4. 프로그램이 중복 실행되면요?**
> OS 레벨 락으로 막습니다. 두 번째 인스턴스는 즉시 종료됩니다(알림 이중 발송 방지).
> 런처도 서비스가 이미 도는지 확인하고 건너뜁니다.

**D5. 죽으면 자동으로 살아납니까?**
> 서비스로 등록하면 크래시 시 15초 후 재시작합니다. 단, **설정 오류로 인한 종료는
> 재시작하지 않습니다** — 무한 재시작하며 Slack을 도배하는 것을 막기 위해서입니다.

**D6. DB는 뭘 씁니까? 백업은요?**
> SQLite 단일 파일(`fds_results.db`)입니다. 파일 복사만으로 백업됩니다.
> 워처와 대시보드가 동시에 접근하므로 WAL 모드로 설정했습니다.
> **다만 무한 증가하므로 보관 정책은 아직 미정입니다** — 남은 일에 올려뒀습니다.

**D7. 로그는 어디에 남습니까?**
> `watcher.log`(워처), `logs\deadman.log`(장애 감시). 서비스 등록 시 자동 로테이션(10MB) 설정됩니다.

---

## E. 데이터 · ML

**E1. 모델 성능이 그 정도면 쓸 만합니까?**
> 사기 비율이 **1.08%인 극불균형**입니다. 이 조건에서 macro F1 0.6138은 낮은 수치가 아닙니다.
> 다만 저희가 더 중요하게 보는 건 **기대비용**입니다 — 미탐 1건이 1,700만원이라
> 성능 최고점(6.30억)보다 비용 최적점(4.62억)을 택했습니다.

**E2. 임계값은 어떻게 정했습니까?**
> **기대비용**으로 정했습니다. 미탐 1건은 보이스피싱 평균 피해액 1,700만원,
> 오탐 1건은 대응비용 5만원 — **340배 차이**입니다. 성능 지표만으로는 판단할 수 없습니다.
> 다만 비용만 최소화하면 임계값이 0으로 밀려 전량 의심처리가 되므로,
> **Macro-F1 ≥ 0.6 제약** 하에서 비용을 최소화해 0.005를 채택했습니다(4.62억).

**E2-1. 왜 하필 Macro-F1 0.6입니까? 0.55면 더 싼데요.** ⭐
> 최종값을 결정한 것이 실제로 이 제약이라 중요한 질문입니다.
> 0.6은 `model_meta.json`에 기록된 **모델 기준선 0.6138**을 지키는 값입니다.
> "성능을 보고된 수준 아래로 떨어뜨리지 않는다"는 정책 결정이고,
> 이 선을 낮추면 더 낮은 임계값이 선택됩니다. 완화 여부는 경영 판단 영역이라고 봅니다.

**E2-2. 0.005와 0.01 중 왜 0.005입니까?**
> 솔직히 **통계적으로 단정하기 어렵습니다.** 두 값의 비용 차이는 0.67억이고
> 미탐 4건 차이인데, 사기 258건 표본에서는 분산 범위일 수 있습니다.
> 신뢰구간 확인이 필요하다고 보고 있고, 그 전까지는 보수적으로 낮은 쪽을 택했습니다.

**E3. 임계값이 실제로 영향이 있긴 합니까?**
> 큽니다. 0.005~0.10 구간에서 미탐이 27건 → 44건으로 변하고 비용은 2.86억 차이가 납니다.
> 다만 **0.1 이상에서는 거의 평평합니다** — 의사결정이 일어나는 구간은 0.05 아래입니다.

**E4. 왜 유형이 사기면 점수가 낮아도 알립니까?**
> 미탐 안전망입니다. 무인 경로에서 놓친 건은 되돌릴 수 없습니다.
> 오탐은 담당자가 5초 보고 넘기면 되지만, 미탐은 사고가 됩니다.

**E5. 못 잡는 건 어떤 것들입니까?**
> 임계값을 0까지 내려도 못 잡는 게 41건이고, 68%가 4개 유형에 몰려 있습니다.
> `l` 기타사기 38.1% · `e` ATM 30.4% · `c` 명의도용 25.9% · `i` 다중시도 26.1%.
> 모델이 "정상"이라고 **확신**하는 건이라 임계값으로는 해결되지 않습니다.

**E6. 그럼 어떻게 개선합니까?**
> 저 4개 유형에 집중해야 합니다. `l`(기타)은 패턴이 이질적인 잡동사니 클래스라
> 분리가 필요할 수 있고, 나머지 3개는 놓치는 피처가 있다는 신호로 봅니다.

**E7. 모델이 바뀌면 뭘 해야 합니까?**
> `verify_bundle` 하니스를 돌리면 **30초 안에** 정상 연결 여부가 판정됩니다.
> 전처리 4경로를 모두 돌려 기준값과 대조합니다.

**E8. 학습 데이터로 시연한 것 아닙니까?**
> 시연은 학습 데이터를 썼고 그래서 위험점수가 1.0으로 나옵니다 — **그건 성능이 아닙니다.**
> 성능 수치는 전부 **검증셋(X_va)** 기준입니다. 두 개를 구분해서 보고 있습니다.

---

## F. 개발 · 유지보수

**F1. 유지보수는 누가 합니까? 담당자가 빠지면요?**
> 그래서 문서를 남겼습니다 — 설치·운영 문서, 파이프라인 구조도,
> 설계 결정 기록 12항목, 트러블슈팅 8건(실제로 겪은 것들).
> 코드에도 "왜 이렇게 했는지"를 주석으로 남겨뒀습니다.

**F2. 코드가 흩어져 있지 않습니까?**
> 판정 로직은 `pipeline/detect_service.py` **한 곳에만** 있습니다.
> 대시보드·워처가 같은 함수를 호출합니다. 복붙했다면 두 곳의 판정이 갈라졌을 겁니다.

**F3. 테스트는 있습니까?**
> **정직하게 말씀드리면 자동화된 회귀 테스트는 아직 없습니다.**
> 대신 검증 도구가 있습니다 — `verify_bundle`(모델 연결), `threshold_report`(임계값),
> `agent_facts`(진단). 각 기능은 시나리오별로 수동 검증했습니다.
> 회귀 테스트는 남은 일에 올려뒀습니다.

**F4. AI가 짠 코드 아닙니까?**
> AI를 도구로 사용했습니다. 다만 **모든 결정과 검증은 사람이 했습니다.**
> 실제로 개발 중 발견해 고친 것들입니다 — 잘못된 파이썬 인터프리터로 인한 더미 모드 사고,
> 배치 파일 인코딩(CP949), 구버전 DB 스키마 마이그레이션, 커서 안정화 로직 결함.
> 모두 **실행 로그를 보고 잡은 것**이고 수정 이유가 코드 주석에 남아 있습니다.

**F5. 왜 이렇게 복잡합니까? 간단히 못 합니까?**
> 복잡한 부분은 대부분 **무인 운영 때문**입니다. 사람이 보고 있으면 필요 없는 것들이죠 —
> 모델 로드 실패 시 기동 거부, LLM 서킷 브레이커, 중복 알림 억제, 데드맨 스위치.
> 사람이 없을 때 조용히 잘못되는 걸 막는 장치들입니다.

**F6. 다른 팀도 쓸 수 있습니까?**
> 판정 엔진이 UI와 분리돼 있어 가능합니다. 데이터 형식(58피처)만 맞으면 되고,
> 어댑터를 추가하는 형태입니다. 이미 대시보드·워처 두 개가 붙어 있습니다.

---

## G. 곤란한 질문

**G1. 이거 없어도 되는 것 아닙니까?**
> 없으면 **야간·주말에 들어온 이상거래를 다음 근무일까지 아무도 못 봅니다.**
> 지금 구조는 담당자가 화면을 열고 버튼을 눌러야 탐지가 시작됩니다.

**G2. 규칙 기반으로 충분하지 않습니까? AI가 꼭 필요합니까?**
> 규칙도 **함께 쓰고 있습니다** — 56개 지표 체크리스트가 판정 근거로 들어갑니다.
> 다만 규칙만으로는 조합 패턴을 잡기 어렵고, 모델과 규칙이 **불일치하면 검토 등급으로 승격**시켜
> 서로를 보완하게 했습니다. LLM은 판정이 아니라 **설명**에만 씁니다.

**G3. LLM이 헛소리하면 그대로 발송되는 것 아닙니까?**
> 판정은 LLM이 하지 않습니다 — ML 모델이 합니다. LLM은 이미 나온 판정을 설명할 뿐입니다.
> LLM이 죽어도 **기본 양식으로 알림은 나갑니다**(3중 폴백).
> 반복 출력 감지, 연속 실패 시 서킷 차단도 있습니다.

**G4. 왜 이걸 혼자 만들었습니까? 상용 솔루션은요?**
> 데이터가 사외로 나가면 안 되는 제약이 있었고,
> 기존 대시보드 자산(모델·전처리·규칙·RAG)을 그대로 재사용할 수 있었습니다.
> 상용 도입 시에도 이 검증 결과가 요구사항 정의에 쓰입니다.

**G5. 오탐이 한 건이라도 나면 담당자가 신뢰하지 않을 텐데요.**
> 하루 3.7건 중 헛알림은 1건 미만입니다. 다만 미탐 1건이 1,700만원이라
> **오탐을 조금 감수하고 미탐을 줄이는 쪽**이 비용상 유리합니다 — 그 균형점이 현재 값입니다.
> 그리고 **왜 걸렸는지 근거를 항상 함께 보냅니다.** 근거 없는 알림이 신뢰를 깎습니다.

**G6. 이거 계속 관리해야 하는 것 아닙니까? 부담 아닙니까?**
> 정기 관리는 **임계값 조정 정도**이고, 대시보드 슬라이더로 5초면 됩니다.
> 나머지는 자동입니다 — 장애 감지, 예약 재개, 로그 로테이션.
> 다만 **DB 보관 정책은 아직 미정**이라 몇 달 뒤 정리가 필요합니다.

**G7. 지금 당장 시연 가능합니까?**
> 가능합니다. 대시보드에서 합성 데이터를 만들어 감시 폴더로 보내면
> **5초 안에 탐지되고 Slack 알림까지** 이어지는 것을 보실 수 있습니다.

---

## H. 답변 태도

| ❌ | ✅ |
|---|---|
| "완벽합니다" | "이 조건에서는 이 수치이고, 이 한계가 있습니다" |
| "그건 안 됩니다" | "지금은 이렇고, 이 조건이면 가능합니다" |
| "아마 될 겁니다" | "확인해서 알려드리겠습니다" |
| 지적받고 인정 | **먼저 한계를 밝히고 계획을 붙임** |

> 모르는 걸 모른다고 말하는 순간이 신뢰가 올라가는 지점입니다.
> 아는 것만 정확히 말하면 자료 전체의 신뢰도가 유지됩니다.
